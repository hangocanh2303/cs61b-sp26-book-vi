# 6. DLLists

Trong Chương 2.2, chúng ta đã xây dựng lớp `SLList`, tốt hơn so với cấu trúc dữ liệu `IntList` đệ quy lộ thiên trước đó. Trong phần này, ta sẽ khép lại chủ đề linked list, đồng thời bắt đầu học những nền tảng về mảng cần thiết để xây dựng một danh sách dựa trên mảng mà ta sẽ gọi là `AList`. Nhân tiện, ta cũng sẽ hé lộ lý do vì sao ở chương trước lại dùng cái tên hơi gượng là `SLList`.

#### addLast <a href="#addlast" id="addlast"></a>

Hãy xem xét phương thức `addLast(int x)` từ chương trước.

```java
public void addLast(int x) {
    size += 1;
    IntNode p = sentinel;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

Vấn đề của phương thức này là nó chậm. Với một danh sách dài, `addLast` phải đi qua toàn bộ danh sách, giống như điều ta đã thấy với phương thức `size` ở chương 2.2. Tương tự, ta có thể thử tăng tốc nó bằng cách thêm biến `last`, như dưới đây:

```java
public class SLList {
    private IntNode sentinel;
    private IntNode last;
    private int size;    

    public void addLast(int x) {
        last.next = new IntNode(x, null);
        last = last.next;
        size += 1;
    }
    ...
}
```

**Bài tập 2.3.1:** Hãy xem sơ đồ hộp-và-con-trỏ biểu diễn cách triển khai `SLList` ở trên, trong đó có thêm con trỏ `last`. Giả sử ta muốn hỗ trợ các thao tác `addLast`, `getLast`, và `removeLast`. Cấu trúc này có hỗ trợ cả ba thao tác đó một cách nhanh chóng không? Nếu không, thao tác nào sẽ chậm?

![sllist\_last\_pointer.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/sllist_last_pointer.png)

**Trả lời 2.3.1:** `addLast` và `getLast` sẽ nhanh, nhưng `removeLast` sẽ chậm. Lý do là ta không có cách dễ dàng để lấy nút áp chót nhằm cập nhật con trỏ `last` sau khi xóa nút cuối cùng.

#### SecondToLast <a href="#secondtolast" id="secondtolast"></a>

Vấn đề với cấu trúc trong bài tập 2.3.1 là một phương thức xóa phần tử cuối cùng của danh sách sẽ vốn dĩ chậm. Lý do là trước hết ta phải tìm nút áp chót, rồi đặt con trỏ `next` của nó thành `null`. Việc thêm một con trỏ `secondToLast` cũng không giúp ích, vì khi đó ta lại phải tìm nút thứ ba từ cuối lên để đảm bảo `secondToLast` và `last` vẫn thỏa mãn các bất biến sau khi xóa phần tử cuối cùng.

**Bài tập 2.3.2:** Hãy thử nghĩ ra một cách để tăng tốc thao tác `removeLast` sao cho nó luôn chạy trong thời gian hằng số, bất kể danh sách dài bao nhiêu. Đừng lo chuyện hiện thực hóa nó bằng mã; ta sẽ để việc đó cho Project 1. Chỉ cần nghĩ ra cách sửa đổi cấu trúc của danh sách, tức là các biến thực thể, là đủ.

Ta sẽ mô tả lời giải trong Cải tiến #7.

#### Cải tiến #7: Nhìn lại <a href="#improvement-7-looking-back" id="improvement-7-looking-back"></a>

Cách tự nhiên nhất để giải quyết vấn đề này là thêm một con trỏ `prev` vào mỗi `IntNode`, tức là:

```java
public class IntNode {
    public IntNode prev;
    public int item;
    public IntNode next;
}
```

Nói cách khác, giờ đây danh sách của ta có hai liên kết cho mỗi nút. Tên gọi phổ biến cho kiểu danh sách này là "Doubly Linked List" (danh sách liên kết đôi), mà ta sẽ viết tắt là `DLList`. Đây là để phân biệt với danh sách liên kết đơn ở chương 2.2, hay `SLList`.

Việc thêm các con trỏ phụ này sẽ làm tăng độ phức tạp của mã. Thay vì hướng dẫn bạn từng bước, bạn sẽ tự xây dựng một danh sách liên kết đôi trong Project 1. Sơ đồ hộp và con trỏ bên dưới cho thấy chính xác hơn hình dạng của danh sách liên kết đôi với kích thước 0 và kích thước 2, tương ứng.

![dllist\_basic\_size\_0.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_basic_size_0.png)

![dllist\_basic\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_basic_size_2.png)

#### Cải tiến #8: Tinh chỉnh sentinel <a href="#improvement-8-sentinel-upgrade" id="improvement-8-sentinel-upgrade"></a>

Các con trỏ ngược cho phép danh sách hỗ trợ việc thêm, lấy và xóa ở cả đầu lẫn cuối trong thời gian hằng số. Tuy nhiên, có một vấn đề tinh vi với thiết kế này: con trỏ `last` đôi khi trỏ tới nút sentinel, và đôi khi lại trỏ tới một nút dữ liệu thật. Cũng giống như phiên bản `SLList` không dùng sentinel, điều này dẫn đến mã có các trường hợp đặc biệt, và mã đó sẽ xấu hơn nhiều so với sau cải tiến thứ 8, cũng là cải tiến cuối cùng của chúng ta. (Bạn có thể nghĩ ra những phương thức nào của `DLList` sẽ gặp các trường hợp đặc biệt này không?)

Một cách khắc phục là thêm nút sentinel thứ hai ở cuối danh sách. Cách này tạo ra cấu trúc liên kết (topology) như trong sơ đồ hộp và con trỏ dưới đây.

![dllist\_double\_sentinel\_size\_0.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_double_sentinel_size_0.png)

![dllist\_double\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_double_sentinel_size_2.png)

Một cách tiếp cận khác là triển khai danh sách dạng vòng (circular), với con trỏ đầu và con trỏ cuối chia sẻ chung một nút sentinel.

![dllist\_circular\_sentinel\_size\_0.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_circular_sentinel_size_0.png)

![dllist\_circular\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_circular_sentinel_size_2.png)

Cả cách dùng hai sentinel lẫn cách dùng sentinel vòng đều hoạt động, và đều cho ra mã không còn các nhánh đặc biệt xấu xí. Dù vậy, cá nhân tôi thấy cách tiếp cận vòng gọn gàng và đẹp hơn về mặt thẩm mỹ. Ta sẽ không đi sâu vào chi tiết cài đặt, vì bạn sẽ có cơ hội tự khám phá một hoặc cả hai trong Project 1.

#### `DLList` và generics <a href="#generic-dllists" id="generic-dllists"></a>

Các `DLList` của ta có một hạn chế lớn: chúng chỉ có thể chứa giá trị kiểu `int`. Ví dụ, giả sử ta muốn tạo một danh sách các chuỗi:

```java
DLList d2 = new DLList("hello");
d2.addLast("world");
```

Đoạn mã trên sẽ bị lỗi, vì constructor `DLList` và các phương thức `addLast` của ta chỉ nhận đối số kiểu số nguyên.

May mắn là vào năm 2004, những người tạo ra Java đã bổ sung **generics** vào ngôn ngữ, cho phép ta (cùng nhiều thứ khác) tạo ra các cấu trúc dữ liệu có thể chứa bất kỳ kiểu tham chiếu nào.

Cú pháp của nó lúc đầu hơi lạ. Ý tưởng cơ bản là ngay sau tên lớp trong phần khai báo lớp, ta đặt vào một tên đại diện tùy ý bên trong dấu ngoặc nhọn `<>`. Sau đó, ở bất cứ đâu muốn dùng kiểu tùy ý đó, ta thay bằng tên đại diện này.

Ví dụ, khai báo `DLList` trước đây của ta là:

```java
public class DLList {
    private IntNode sentinel;
    private int size;

    public class IntNode {
        public IntNode prev;
        public int item;
        public IntNode next;
        ...
    }
    ...
}
```

Một `DLList` dùng generics (có thể chứa bất kỳ kiểu tham chiếu nào) sẽ trông như sau:

```java
public class DLList<BleepBlorp> {
    private IntNode sentinel;
    private int size;

    public class IntNode {
        public IntNode prev;
        public BleepBlorp item;
        public IntNode next;
        ...
    }
    ...
}
```

Ở đây, `BleepBlorp` chỉ là cái tên tôi bịa ra, và bạn hoàn toàn có thể dùng gần như bất kỳ tên nào khác, chẳng hạn `GloopGlop`, `Horse`, `TelbudorphMulticulus`, v.v.

Giờ ta đã định nghĩa một phiên bản `DLList` có tham số kiểu (type parameter), nên cũng phải dùng cú pháp đặc biệt khi khởi tạo. Cụ thể, ta đặt kiểu mong muốn trong dấu ngoặc nhọn khi khai báo biến, và dùng cặp ngoặc nhọn rỗng khi khởi tạo. Ví dụ:

```java
DLList<String> d2 = new DLList<>("hello");
d2.addLast("world");
```

Vì generics chỉ hoạt động với kiểu tham chiếu, ta không thể đặt các kiểu nguyên thủy như `int` hay `double` vào trong dấu ngoặc nhọn, ví dụ `<int>`. Thay vào đó, ta dùng phiên bản tham chiếu của kiểu nguyên thủy đó; chẳng hạn với `int` thì dùng `Integer`, ví dụ:

```java
DLList<Integer> d1 = new DLList<>(5);
d1.insertFront(10);
```

Còn khá nhiều thứ nữa khi làm việc với generics, nhưng ta sẽ để dành cho một chương sau, khi bạn đã có thêm cơ hội tự mình thực hành. Hiện tại, hãy dùng các quy tắc thực hành sau:

* Trong tệp `.java` **đang triển khai** một cấu trúc dữ liệu, chỉ khai báo tham số kiểu một lần ngay sau tên lớp.
* Trong các tệp `.java` khác **dùng** cấu trúc dữ liệu đó, hãy ghi rõ kiểu thực tế khi khai báo biến và dùng toán tử diamond rỗng (`<>`) khi khởi tạo.
* Nếu bạn cần tham số kiểu tương ứng với kiểu nguyên thủy, hãy dùng `Integer`, `Double`, `Character`, `Boolean`, `Long`, `Short`, `Byte` hoặc `Float` thay cho kiểu nguyên thủy tương ứng.

Chi tiết nhỏ: bạn cũng có thể ghi rõ kiểu bên trong cặp ngoặc nhọn khi khởi tạo, dù điều này không bắt buộc nếu bạn đồng thời đang khai báo biến trên cùng dòng. Nói cách khác, dòng mã sau hoàn toàn hợp lệ, dù `Integer` ở vế phải là dư thừa.

```java
DLList<Integer> d1 = new DLList<Integer>(5);
```

Đến đây, bạn đã biết mọi thứ cần thiết để triển khai dự án `LinkedListDeque` trong Project 1, nơi bạn sẽ củng cố toàn bộ kiến thức đã tích lũy ở các chương 2.1, 2.2 và 2.3.

# 6. Mảng

Cho đến nay, ta đã thấy cách tận dụng các định nghĩa lớp đệ quy để tạo lớp danh sách có thể mở rộng, bao gồm `IntList`, `SLList` và `DLList`. Trong hai phần tiếp theo của cuốn sách, ta sẽ bàn về cách xây dựng một lớp danh sách sử dụng mảng.

Phần này giả định bạn đã từng làm việc với mảng và không nhằm làm hướng dẫn toàn diện về cú pháp mảng.

### Khái niệm cơ bản về mảng <a href="#array-basics" id="array-basics"></a>

Để cuối cùng có thể xây dựng một danh sách chứa thông tin, ta cần một cách nào đó để có được các ô nhớ. Trước đây, ta đã biết cách có ô nhớ thông qua khai báo biến và khởi tạo đối tượng. Ví dụ:

* `int x;` cho ta một ô nhớ 32 bit dùng để lưu `int`.
* `Walrus w1;` cho ta một ô nhớ 64 bit dùng để lưu tham chiếu tới `Walrus`.
* `Walrus w2 = new Walrus(30, 5.6);` cho ta tổng cộng 3 ô nhớ: một ô 64 bit lưu tham chiếu `Walrus`, một ô 32 bit lưu `int` biểu diễn kích thước (size) của `Walrus`, và một ô 64 bit lưu `double` biểu diễn `tuskSize` của `Walrus`.

Mảng là một loại đối tượng đặc biệt gồm một dãy các ô nhớ được đánh số. Điều này khác với các thực thể (instance) của lớp, vốn có các ô nhớ được đặt tên. Để lấy phần tử thứ `i` của một mảng, ta dùng ký hiệu ngoặc vuông như đã thấy trong HW0 và Project 0, ví dụ `A[i]` để lấy phần tử thứ `i` của `A`.

Mảng bao gồm:

* Một độ dài cố định N (kiểu số nguyên).
* Một dãy N ô nhớ (N = độ dài), tất cả các ô cùng kiểu, được đánh số từ 0 đến N − 1.

Khác với lớp, mảng không có phương thức.

#### AList thô sơ

Trong phần này, ta sẽ xây dựng một lớp mới tên là `AList` dùng để lưu danh sách dữ liệu dài tùy ý, tương tự `DLList`. Khác với `DLList`, `AList` sẽ dùng mảng để lưu dữ liệu thay vì danh sách liên kết.

#### Câu đố về hiệu suất danh sách liên kết <a href="#linked-list-performance-puzzle" id="linked-list-performance-puzzle"></a>

Giả sử ta muốn viết một phương thức mới cho `DLList` có tên `int get(int i)`. Tại sao `get` lại chậm với danh sách dài so với `getLast`? Với đầu vào nào thì nó sẽ đặc biệt chậm?

Hình dưới đây có thể giúp bạn suy nghĩ về câu trả lời.

![dllist\_circular\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_circular_sentinel_size_2.png)

#### Lời giải cho câu đố hiệu năng của linked list <a href="#linked-list-performance-puzzle-solution" id="linked-list-performance-puzzle-solution"></a>

Hóa ra là dù bạn có thông minh đến đâu, phương thức `get` nói chung vẫn sẽ chậm hơn `getBack` nếu ta dùng cấu trúc danh sách liên kết đôi mô tả ở phần 2.3.

Lý do là vì ta chỉ có tham chiếu tới phần tử đầu và phần tử cuối của danh sách, nên để lấy phần tử cần truy xuất, ta luôn phải đi qua danh sách từ đầu hoặc từ cuối. Ví dụ, nếu muốn lấy phần tử số 417 trong một danh sách dài 10.000, ta sẽ phải đi qua 417 liên kết theo chiều tiến để tới đúng phần tử đó.

Trong trường hợp xấu nhất, phần tử cần tìm nằm ngay giữa danh sách, và ta sẽ phải đi qua một số lượng phần tử tỉ lệ với độ dài của danh sách, cụ thể là khoảng một nửa số phần tử. Nói cách khác, thời gian chạy trong trường hợp xấu nhất của `get` là tuyến tính theo kích thước của toàn bộ danh sách. Điều này trái ngược với `getBack`, vốn có thời gian chạy hằng số bất kể danh sách dài bao nhiêu. Ở phần sau của môn học, ta sẽ định nghĩa chính thức các thời gian chạy bằng ký hiệu big-O và big-Theta. Hiện tại, cứ hiểu theo cách không chính thức là đủ.

#### Nỗ lực đầu tiên của chúng ta: danh sách dựa trên mảng thô sơ <a href="#our-first-attempt-the-naive-array-based-list" id="our-first-attempt-the-naive-array-based-list"></a>

Việc truy cập phần tử thứ `i` của một mảng mất thời gian hằng số trên máy tính hiện đại. Điều này gợi ý rằng một danh sách dựa trên mảng có thể cho hiệu năng của `get` tốt hơn nhiều so với cách cài đặt bằng linked list, vì nó chỉ cần dùng ký hiệu ngoặc để lấy phần tử cần quan tâm.

Nếu bạn muốn biết **tại sao** mảng có thời gian truy cập hằng số, hãy xem [bài đăng trên Quora](https://www.quora.com/Why-does-accessing-an-array-element-take-constant-time) này.

#### removeLast <a href="#removelast" id="removelast"></a>

Thao tác cuối cùng mà ta cần hỗ trợ là `removeLast`. Trước khi bắt đầu, hãy ghi nhận một quan sát then chốt: bất kỳ thay đổi nào đối với danh sách đều phải được phản ánh bằng thay đổi ở một hay nhiều ô nhớ trong cài đặt của ta.

Điều này nghe có vẻ hiển nhiên, nhưng nó khá sâu sắc. Danh sách là một khái niệm trừu tượng, còn các ô nhớ `size`, `items`, và `items[i]` là biểu diễn cụ thể của khái niệm đó. Mọi thay đổi mà người dùng muốn thực hiện trên danh sách thông qua các phương thức trừu tượng ta cung cấp (`addLast`, `removeLast`) đều phải được phản ánh thành những thay đổi cụ thể trên các ô nhớ này, theo cách phù hợp với kỳ vọng của người dùng. Chính các bất biến sẽ chỉ dẫn cho ta biết những thay đổi đó nên trông như thế nào.


### Phụ lục: Thông tin bổ sung về mảng

Có ba ký hiệu hợp lệ để tạo mảng. Hãy thử chạy mã bên dưới và xem điều gì sẽ xảy ra. Nhấp vào [đây](http://pythontutor.com/iframe-embed.html#code=public%20class%20ArrayCreationDemo%20%7B%0A%20%20public%20static%20void%20main\(String%5B%5D%20args%29%20%7B%0A%20%20%20%20int%5B%5D%20x%3B%0A%20%20%20%20int%5B%5D%20y%3B%0A%20%20%20%20x%20%3D%20new%20int%5B3%5D%3B%0A%20%20%20%20y%20%3D%20new%20int%5B%5D%7B1,%202,%203,%204,%205%7D%3B%0A%20%20%20%20int%5B%5D%20z%20%3D%20%7B9,%2010,%2011,%2012,%2013%7D%3B%0A%09%7D%0A%7D\&codeDivHeight=400\&codeDivWidth=350\&cumulative=false\&curInstr=0\&heapPrimitives=false\&origin=opt-frontend.js\&py=java\&rawInputLstJSON=%5B%5D\&textReferences=false) để hiển thị tương tác.

* `x = new int[3];`
* `y = new int[]{1, 2, 3, 4, 5};`
* `int[] z = {9, 10, 11, 12, 13};`

Cả ba cách đều tạo ra một mảng. Cách thứ nhất, dùng để tạo `x`, sẽ tạo mảng có độ dài được chỉ định và điền vào mỗi ô nhớ giá trị mặc định. Ở đây, nó tạo mảng độ dài 3 và điền cả 3 ô với giá trị `int` mặc định là `0`.

Cách thứ hai, dùng để tạo `y`, tạo mảng có kích thước vừa đủ để chứa các giá trị khởi tạo đã cho. Ở đây, nó tạo mảng độ dài 5 với năm phần tử cụ thể đó.

Cách thứ ba, dùng để **vừa khai báo vừa tạo** `z`, có hành vi giống cách thứ hai. Điểm khác biệt duy nhất là nó bỏ qua từ khóa `new`, và chỉ có thể dùng khi kết hợp với khai báo biến.

Không cách nào tốt hơn cách nào.

#### Truy cập và sửa đổi mảng <a href="#array-access-and-modification" id="array-access-and-modification"></a>

Đoạn mã sau trình bày toàn bộ cú pháp chính mà ta sẽ dùng để làm việc với mảng. Hãy thử chạy từng dòng mã bên dưới và đảm bảo bạn hiểu chuyện gì xảy ra khi mỗi dòng thực thi. Để làm điều đó, hãy nhấp vào [đây](https://goo.gl/bertuh) để xem trực quan hóa tương tác. Trừ dòng cuối cùng, ta đã gặp tất cả các cú pháp này từ trước.

```java
int[] z = null;
int[] x, y;

x = new int[]{1, 2, 3, 4, 5};
y = x;
x = new int[]{-1, 2, 5, 4, 99};
y = new int[3];
z = new int[0];
int xL = x.length;

String[] s = new String[6];
s[4] = "ketchup";
s[x[3] - x[1]] = "muffins";

int[] b = {9, 10, 11};
System.arraycopy(b, 0, x, 3, 2);
```

Dòng cuối cùng minh họa một cách sao chép dữ liệu từ mảng này sang mảng khác. `System.arraycopy` nhận năm tham số:

* Mảng nguồn
* Vị trí bắt đầu trong mảng nguồn
* Mảng đích
* Vị trí bắt đầu trong mảng đích
* Số phần tử cần sao chép

Với những ai quen Python, `System.arraycopy(b, 0, x, 3, 2)` tương đương với `x[3:5] = b[0:2]` trong Python.

Một cách khác để sao chép mảng là dùng vòng lặp. `arraycopy` thường nhanh hơn vòng lặp và cho ra mã gọn hơn. Nhược điểm duy nhất là `arraycopy` (có thể nói là) khó đọc hơn. Lưu ý rằng mảng Java chỉ kiểm tra giới hạn chỉ số (bounds checking) lúc chạy. Nghĩa là đoạn mã sau biên dịch bình thường, nhưng sẽ crash khi chạy.

```java
int[] x = {9, 10, 11, 12, 13};
int[] y = new int[2];
int i = 0;
while (i < x.length) {
    y[i] = x[i];
    i += 1;
}
```

Hãy thử chạy đoạn mã này trên máy hoặc trong [trình trực quan hóa](https://goo.gl/YHufJ6). Tên của lỗi mà bạn gặp khi nó crash là gì? Tên đó có hợp lý không?

#### Mảng 2D trong Java <a href="#id-2d-arrays-in-java" id="id-2d-arrays-in-java"></a>

Cái mà người ta có thể gọi là mảng 2D trong Java thực chất chỉ là một mảng các mảng. Chúng tuân theo cùng các quy tắc đối tượng mà ta đã học, nhưng hãy ôn lại để chắc chắn ta hiểu cách chúng hoạt động.

Cú pháp cho mảng của mảng có thể hơi rối. Hãy xét đoạn mã `int[][] bamboozle = new int[4][]`. Dòng này tạo ra một mảng các mảng số nguyên tên là `bamboozle`. Cụ thể, nó tạo ra đúng bốn ô nhớ, mỗi ô có thể trỏ tới một mảng số nguyên (có độ dài chưa xác định).

Hãy thử chạy đoạn mã dưới đây từng dòng một và xem kết quả có khớp với trực giác của bạn không. Để xem trực quan hóa tương tác, hãy nhấp vào [đây](http://goo.gl/VS4cOK).

```java
int[][] pascalsTriangle;
pascalsTriangle = new int[4][];
int[] rowZero = pascalsTriangle[0];

pascalsTriangle[0] = new int[]{1};
pascalsTriangle[1] = new int[]{1, 1};
pascalsTriangle[2] = new int[]{1, 2, 1};
pascalsTriangle[3] = new int[]{1, 3, 3, 1};
int[] rowTwo = pascalsTriangle[2];
rowTwo[1] = -5;

int[][] matrix;
matrix = new int[4][];
matrix = new int[4][4];

int[][] pascalAgain = new int[][]{{1}, {1, 1},
                                 {1, 2, 1}, {1, 3, 3, 1}};
```

**Bài tập 2.4.1:** Sau khi chạy đoạn mã bên dưới, giá trị của x\[0]\[0] và w\[0]\[0] sẽ là bao nhiêu? Đối chiếu đáp án bằng cách nhấp vào [đây](http://goo.gl/fCZ9Dr).

```java
int[][] x = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};

int[][] z = new int[3][];
z[0] = x[0];
z[1] = x[1];
z[2] = x[2];
z[0][0] = -z[0][0];

int[][] w = new int[3][3];
System.arraycopy(x[0], 0, w[0], 0, 3);
System.arraycopy(x[1], 0, w[1], 0, 3);
System.arraycopy(x[2], 0, w[2], 0, 3);
w[0][0] = -w[0][0];
```

#### Mảng so với lớp <a href="#arrays-vs-classes" id="arrays-vs-classes"></a>

Cả mảng và lớp đều có thể dùng để tổ chức một nhóm ô nhớ. Trong cả hai trường hợp, số lượng ô nhớ là cố định: không thể thay đổi độ dài mảng, cũng không thể thêm hay xóa trường của lớp.

Các khác biệt chính giữa ô nhớ trong mảng và trong lớp:

* Ô nhớ của mảng được đánh số và truy cập bằng ký hiệu `[]`, còn ô nhớ của lớp được đặt tên và truy cập bằng ký hiệu dấu chấm.
* Tất cả các ô của mảng phải cùng kiểu. Các ô của lớp có thể khác kiểu nhau.

Một hệ quả đáng chú ý của những khác biệt này là ký hiệu `[]` cho phép ta chỉ định chỉ mục cần truy cập tại thời điểm chạy (runtime). Ví dụ, xét đoạn mã sau:

```java
int indexOfInterest = askUserForInteger();
int[] x = {100, 101, 102, 103};
int k = x[indexOfInterest];
System.out.println(k);
```

Nếu chạy đoạn mã này, ta có thể nhận được kết quả như sau:

```
$ javac arrayDemo
$ java arrayDemo
What index do you want? 2
102
```

Ngược lại, việc chọn trường của một lớp không phải thứ ta làm tại thời điểm chạy. Ví dụ, xét đoạn mã sau:

```java
String fieldOfInterest = "mass";
Planet p = new Planet(6e24, "earth");
double mass = p[fieldOfInterest];
```

Nếu thử biên dịch đoạn này, ta sẽ gặp lỗi cú pháp.

```
$ javac classDemo
FieldDemo.java:5: error: array required, but Planet found
        double mass = earth[fieldOfInterest];        
                               ^
```

Vấn đề tương tự xảy ra nếu ta thử dùng ký hiệu dấu chấm:

```java
String fieldOfInterest = "mass";
Planet p = new Planet(6e24, "earth");
double mass = p.fieldOfInterest;
```

Khi biên dịch, ta sẽ nhận được:

```
$ javac classDemo
FieldDemo.java:5: error: cannot find symbol
        double mass = earth.fieldOfInterest;        
                           ^
  symbol:   variable fieldOfInterest
  location: variable earth of type Planet
```

Đây không phải hạn chế mà bạn sẽ thường xuyên gặp, nhưng đáng nêu ra cho chặt chẽ về mặt học thuật. Thực ra, có một cách để chỉ định trường muốn truy cập tại thời điểm chạy gọi là _reflection_, nhưng nó được xem là phong cách lập trình rất tệ cho các chương trình thông thường. Bạn có thể đọc thêm về reflection [ở đây](https://docs.oracle.com/javase/tutorial/reflect/member/fieldValues.html). **Bạn không nên dùng reflection trong bất kỳ chương trình 61B nào**, và ta sẽ không thảo luận về nó trong khóa học.

Nhìn chung, ngôn ngữ lập trình được thiết kế một phần nhằm giới hạn các lựa chọn của lập trình viên, giúp mã dễ suy luận hơn. Bằng cách gom các tính năng kiểu này vào API Reflection riêng, các chương trình Java thông thường trở nên dễ đọc và dễ hiểu hơn.

#### Phụ lục: Mảng Java so với các ngôn ngữ khác <a href="#appendix-java-arrays-vs-other-languages" id="appendix-java-arrays-vs-other-languages"></a>

So với mảng trong các ngôn ngữ khác, mảng Java:

* Không có cú pháp đặc biệt cho "slicing" (như trong Python).
* Không thể co lại hoặc mở rộng (như trong Ruby).
* Không có phương thức thành viên (như trong Javascript).
* Phải chứa các giá trị cùng kiểu (khác với Python).
