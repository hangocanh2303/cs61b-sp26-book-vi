# 6. DLLists

Trong Chương 2.2, chúng ta đã xây dựng lớp `SLList`, tốt hơn so với cấu trúc dữ liệu `IntList` đệ quy lộ thiên trước đó. Trong phần này, ta sẽ khép lại chủ đề linked list, đồng thời bắt đầu học những nền tảng về mảng cần thiết để xây dựng một danh sách dựa trên mảng mà ta sẽ gọi là `AList`. Nhân tiện, ta cũng sẽ hé lộ lý do vì sao ở chương trước lại dùng cái tên hơi gượng là `SLList`.

#### addLast <a href="#addlast" id="addlast"></a>

Hãy xem xét phương thức `addLast(int x)` từ chương trước.

```java
public void addLast(int x) {
    size += 1;
    IntNode p = sentinel;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

Vấn đề của phương thức này là nó chậm. Với một danh sách dài, `addLast` phải đi qua toàn bộ danh sách, giống như điều ta đã thấy với phương thức `size` ở chương 2.2. Tương tự, ta có thể thử tăng tốc nó bằng cách thêm biến `last`, như dưới đây:

```java
public class SLList {
    private IntNode sentinel;
    private IntNode last;
    private int size;    

    public void addLast(int x) {
        last.next = new IntNode(x, null);
        last = last.next;
        size += 1;
    }
    ...
}
```

**Bài tập 2.3.1:** Hãy xem sơ đồ hộp-và-con-trỏ biểu diễn cách triển khai `SLList` ở trên, trong đó có thêm con trỏ `last`. Giả sử ta muốn hỗ trợ các thao tác `addLast`, `getLast`, và `removeLast`. Cấu trúc này có hỗ trợ cả ba thao tác đó một cách nhanh chóng không? Nếu không, thao tác nào sẽ chậm?

![sllist\_last\_pointer.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/sllist_last_pointer.png)

**Trả lời 2.3.1:** `addLast` và `getLast` sẽ nhanh, nhưng `removeLast` sẽ chậm. Lý do là ta không có cách dễ dàng để lấy nút áp chót nhằm cập nhật con trỏ `last` sau khi xóa nút cuối cùng.

#### SecondToLast <a href="#secondtolast" id="secondtolast"></a>

Vấn đề với cấu trúc trong bài tập 2.3.1 là một phương thức xóa phần tử cuối cùng của danh sách sẽ vốn dĩ chậm. Lý do là trước hết ta phải tìm nút áp chót, rồi đặt con trỏ `next` của nó thành `null`. Việc thêm một con trỏ `secondToLast` cũng không giúp ích, vì khi đó ta lại phải tìm nút thứ ba từ cuối lên để đảm bảo `secondToLast` và `last` vẫn thỏa mãn các bất biến sau khi xóa phần tử cuối cùng.

**Bài tập 2.3.2:** Hãy thử nghĩ ra một cách để tăng tốc thao tác `removeLast` sao cho nó luôn chạy trong thời gian hằng số, bất kể danh sách dài bao nhiêu. Đừng lo chuyện hiện thực hóa nó bằng mã; ta sẽ để việc đó cho Project 1. Chỉ cần nghĩ ra cách sửa đổi cấu trúc của danh sách, tức là các biến thực thể, là đủ.

Ta sẽ mô tả lời giải trong Cải tiến #7.

#### Cải tiến #7: Nhìn lại <a href="#improvement-7-looking-back" id="improvement-7-looking-back"></a>

Cách tự nhiên nhất để giải quyết vấn đề này là thêm một con trỏ `prev` vào mỗi `IntNode`, tức là:

```java
public class IntNode {
    public IntNode prev;
    public int item;
    public IntNode next;
}
```

Nói cách khác, giờ đây danh sách của ta có hai liên kết cho mỗi nút. Tên gọi phổ biến cho kiểu danh sách này là "doubly linked list", mà ta sẽ viết tắt là `DLList`. Điều này đối lập với linked list đơn ở chương 2.2, hay `SLList`.

Việc bổ sung các con trỏ bổ sung này sẽ làm tăng độ phức tạp của mã. Thay vì hướng dẫn bạn thực hiện nó, bạn sẽ tự xây dựng một danh sách liên kết đôi trong dự án 1. Sơ đồ hộp và con trỏ bên dưới hiển thị chính xác hơn danh sách liên kết đôi trông như thế nào đối với các danh sách có kích thước 0 và kích thước 2 tương ứng.

![dllist\_basic\_size\_0.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_basic_size_0.png)

![dllist\_basic\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_basic_size_2.png)

#### Cải tiến #8: Tinh chỉnh sentinel <a href="#improvement-8-sentinel-upgrade" id="improvement-8-sentinel-upgrade"></a>

Các con trỏ ngược cho phép danh sách hỗ trợ việc thêm, lấy và xóa ở cả đầu lẫn cuối trong thời gian hằng số. Tuy nhiên, có một vấn đề tinh vi với thiết kế này: con trỏ `last` đôi khi trỏ tới nút sentinel, và đôi khi lại trỏ tới một nút dữ liệu thật. Cũng giống như phiên bản `SLList` không dùng sentinel, điều này dẫn đến mã có các trường hợp đặc biệt, và mã đó sẽ xấu hơn nhiều so với sau cải tiến thứ 8, cũng là cải tiến cuối cùng của chúng ta. (Bạn có thể nghĩ ra những phương thức nào của `DLList` sẽ gặp các trường hợp đặc biệt này không?)

Một cách khắc phục là thêm nút sentinel thứ hai ở cuối danh sách. Điều này dẫn đến cấu trúc liên kết được hiển thị bên dưới dạng sơ đồ hộp và mũi tên.

![dllist\_double\_sentinel\_size\_0.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_double_sentinel_size_0.png)

![dllist\_double\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_double_sentinel_size_2.png)

Một cách tiếp cận khác là triển khai danh sách dạng vòng, với các con trỏ `prev` và `next` gặp nhau tại một nút sentinel chung.

![dllist\_circular\_sentinel\_size\_0.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_circular_sentinel_size_0.png)

![dllist\_circular\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_circular_sentinel_size_2.png)

Cả cách dùng hai sentinel lẫn cách dùng sentinel vòng tròn đều hoạt động, và đều cho ra mã không còn các trường hợp đặc biệt xấu xí. Dù vậy, cá nhân tôi thấy cách tiếp cận vòng tròn gọn và đẹp hơn về mặt thẩm mỹ. Chúng ta sẽ không đi sâu vào chi tiết các cách cài đặt này, vì bạn sẽ có cơ hội tự khám phá một hoặc cả hai trong Project 1.

#### `DLList` và generics <a href="#generic-dllists" id="generic-dllists"></a>

Các `DLList` của ta có một hạn chế lớn: chúng chỉ có thể chứa giá trị kiểu `int`. Ví dụ, giả sử ta muốn tạo một danh sách các chuỗi:

```java
DLList d2 = new DLList("hello");
d2.addLast("world");
```

Đoạn mã trên sẽ bị lỗi, vì constructor `DLList` và các phương thức `addLast` của ta chỉ nhận đối số kiểu số nguyên.

May mắn là vào năm 2004, những người tạo ra Java đã thêm **generics** vào ngôn ngữ. Điều này cho phép ta, trong số những thứ khác, tạo ra các cấu trúc dữ liệu có thể chứa bất kỳ kiểu tham chiếu nào.

Cú pháp của nó lúc đầu hơi lạ. Ý tưởng cơ bản là ngay sau tên lớp trong phần khai báo lớp, ta đặt vào một tên đại diện tùy ý bên trong dấu ngoặc nhọn `<>`. Sau đó, ở bất cứ đâu muốn dùng kiểu tùy ý đó, ta thay bằng tên đại diện này.

Ví dụ, khai báo `DLList` trước đây của ta là:

```java
public class DLList {
    private IntNode sentinel;
    private int size;

    public class IntNode {
        public IntNode prev;
        public int item;
        public IntNode next;
        ...
    }
    ...
}
```

Một `DLList` dùng generics (có thể chứa bất kỳ kiểu tham chiếu nào) sẽ trông như sau:

```java
public class DLList<BleepBlorp> {
    private IntNode sentinel;
    private int size;

    public class IntNode {
        public IntNode prev;
        public BleepBlorp item;
        public IntNode next;
        ...
    }
    ...
}
```

Ở đây, `BleepBlorp` chỉ là tên tôi nghĩ ra và bạn có thể sử dụng hầu hết mọi tên khác mà bạn muốn sử dụng thay thế, chẳng hạn như `GloopGlop`, `Horse`, `TelbudorphMulticulus` hoặc bất cứ tên nào.

Giờ ta đã định nghĩa một phiên bản `DLList` có tham số kiểu (type parameter), nên cũng phải dùng cú pháp đặc biệt khi khởi tạo. Cụ thể, ta đặt kiểu mong muốn trong dấu ngoặc nhọn khi khai báo biến, và dùng cặp ngoặc nhọn rỗng khi khởi tạo. Ví dụ:

```java
DLList<String> d2 = new DLList<>("hello");
d2.addLast("world");
```

Vì generics chỉ hoạt động với kiểu tham chiếu, ta không thể đặt các kiểu nguyên thủy như `int` hay `double` vào trong dấu ngoặc nhọn, ví dụ `<int>`. Thay vào đó, ta dùng phiên bản tham chiếu của kiểu nguyên thủy đó; chẳng hạn với `int` thì dùng `Integer`, ví dụ:

```java
DLList<Integer> d1 = new DLList<>(5);
d1.insertFront(10);
```

Còn khá nhiều sắc thái nữa khi làm việc với generics, nhưng ta sẽ để dành cho một chương sau, khi bạn đã có thêm cơ hội tự mình thực hành. Hiện tại, hãy dùng các quy tắc thực hành sau:

* Trong tệp `.java` **đang triển khai** một cấu trúc dữ liệu, chỉ khai báo tham số kiểu một lần ngay sau tên lớp.
* Trong các tệp `.java` khác **dùng** cấu trúc dữ liệu đó, hãy ghi rõ kiểu thực tế khi khai báo biến và dùng toán tử diamond rỗng (`<>`) khi khởi tạo.
* Nếu bạn cần tham số kiểu tương ứng với kiểu nguyên thủy, hãy dùng `Integer`, `Double`, `Character`, `Boolean`, `Long`, `Short`, `Byte` hoặc `Float` thay cho kiểu nguyên thủy tương ứng.

Chi tiết nhỏ: bạn cũng có thể ghi rõ kiểu trong cặp `<>` ở vế `new`, dù thường không cần nếu cùng một dòng đã khai báo biến với kiểu đầy đủ. Ví dụ dòng sau hợp lệ, dù `Integer` ở vế phải là dư thừa.

```java
DLList<Integer> d1 = new DLList<Integer>(5);
```

Đến đây, bạn đã biết mọi thứ cần thiết để triển khai dự án `LinkedListDeque` trong Project 1, nơi bạn sẽ củng cố toàn bộ kiến thức đã tích lũy ở các chương 2.1, 2.2 và 2.3.

# 6. Mảng

Cho đến nay, chúng ta đã thấy cách khai thác các định nghĩa lớp đệ quy để tạo một lớp danh sách có thể mở rộng, bao gồm `IntList`, `SLList` và `DLList`. Trong hai phần tiếp theo của cuốn sách này, chúng ta sẽ thảo luận cách xây dựng một lớp danh sách bằng cách sử dụng mảng.

Phần này giả định rằng bạn đã từng làm việc với mảng trước đó và không nhằm mục đích trở thành một hướng dẫn đầy đủ về cú pháp mảng.

### Khái niệm cơ bản về mảng <a href="#array-basics" id="array-basics"></a>

Cuối cùng, để xây dựng một danh sách có thể chứa thông tin, chúng ta cần một số cách để lấy được các hộp bộ nhớ. Trước đây, chúng ta đã biết cách lấy các hộp bộ nhớ với các khai báo biến và khởi tạo lớp. Ví dụ:

* `int x;` cho chúng ta một hộp bộ nhớ 32 bit để lưu trữ các số nguyên.
* `Walrus w1;` cho ta một hộp bộ nhớ 64 bit dùng để lưu tham chiếu tới `Walrus`.
* `Walrus w2 = new Walrus(30, 5.6);` cho ta tổng cộng 3 hộp bộ nhớ: một hộp 64 bit lưu tham chiếu `Walrus`, một hộp 32 bit lưu `int` biểu diễn kích thước của `Walrus`, và một hộp 64 bit lưu `double` biểu diễn `tuskSize`.

Mảng là một loại đối tượng đặc biệt gồm một dãy các hộp bộ nhớ được đánh số. Điều này khác với các thể hiện của lớp, vốn có các hộp bộ nhớ được đặt tên. Để lấy phần tử thứ `i` của một mảng, ta dùng ký hiệu ngoặc vuông như đã thấy trong HW0 và Project 0, ví dụ `A[i]` để lấy phần tử thứ `i` của `A`.

Mảng bao gồm:

* Độ dài số nguyên cố định, N
* Một chuỗi N hộp bộ nhớ (N = chiều dài) trong đó tất cả các hộp đều cùng loại và được đánh số từ 0 đến N - 1.

Không giống như lớp, mảng không có phương thức.

#### AList thô sơ

Trong phần này, chúng ta sẽ xây dựng một lớp mới gọi là `AList` có thể được sử dụng để lưu trữ danh sách dữ liệu dài tùy ý, tương tự như `DLList` của chúng ta. Không giống như `DLList`, `AList` sẽ sử dụng mảng để lưu trữ dữ liệu thay vì danh sách liên kết.

#### Câu đố về hiệu suất danh sách liên kết <a href="#linked-list-performance-puzzle" id="linked-list-performance-puzzle"></a>

Giả sử chúng ta muốn viết một phương thức mới cho `DLList` gọi là `int get(int i)`. Tại sao `get` lại chậm đối với danh sách dài so với `getLast`? Đối với đầu vào nào nó sẽ đặc biệt chậm?

Bạn có thể thấy hình dưới đây hữu ích cho việc suy nghĩ về câu trả lời của mình.

![dllist\_circular\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig23/dllist_circular_sentinel_size_2.png)

#### Lời giải cho câu đố hiệu năng của linked list <a href="#linked-list-performance-puzzle-solution" id="linked-list-performance-puzzle-solution"></a>

Hóa ra là dù bạn có thông minh đến đâu thì phương thức `get` thường sẽ chậm hơn `getBack` nếu chúng ta đang sử dụng cấu trúc danh sách liên kết đôi được mô tả trong phần 2.3.

Lý do là vì ta chỉ có tham chiếu tới phần tử đầu và phần tử cuối của danh sách, nên để lấy phần tử cần truy xuất, ta luôn phải đi qua danh sách từ đầu hoặc từ cuối. Ví dụ, nếu muốn lấy phần tử số 417 trong một danh sách dài 10.000, ta sẽ phải đi qua 417 liên kết theo chiều tiến để tới đúng phần tử đó.

Trong trường hợp xấu nhất, phần tử cần tìm nằm ngay giữa danh sách, và ta sẽ phải đi qua một số lượng phần tử tỉ lệ với độ dài của danh sách, cụ thể là khoảng một nửa số phần tử. Nói cách khác, thời gian chạy trong trường hợp xấu nhất của `get` là tuyến tính theo kích thước của toàn bộ danh sách. Điều này trái ngược với `getBack`, vốn có thời gian chạy hằng số bất kể danh sách dài bao nhiêu. Ở phần sau của môn học, ta sẽ định nghĩa chính thức các thời gian chạy bằng ký hiệu big-O và big-Theta. Hiện tại, cứ hiểu theo cách không chính thức là đủ.

#### Nỗ lực đầu tiên của chúng ta: danh sách dựa trên mảng thô sơ <a href="#our-first-attempt-the-naive-array-based-list" id="our-first-attempt-the-naive-array-based-list"></a>

Việc truy cập phần tử thứ `i` của một mảng mất thời gian hằng số trên máy tính hiện đại. Điều này gợi ý rằng một danh sách dựa trên mảng có thể cho hiệu năng của `get` tốt hơn nhiều so với cách cài đặt bằng linked list, vì nó chỉ cần dùng ký hiệu ngoặc để lấy phần tử cần quan tâm.

Nếu bạn muốn biết **tại sao** mảng có quyền truy cập theo thời gian liên tục, hãy xem [bài đăng trên Quora](https://www.quora.com/Why-does-accessing-an-array-element-take-constant-time) này.

#### removeLast <a href="#removelast" id="removelast"></a>

Thao tác cuối cùng mà ta cần hỗ trợ là `removeLast`. Trước khi bắt đầu, hãy lưu ý một nhận xét quan trọng: bất kỳ thay đổi nào đối với danh sách đều phải được phản ánh bằng thay đổi ở một hay nhiều hộp bộ nhớ trong cách cài đặt của ta.

Điều này nghe có vẻ hiển nhiên, nhưng nó lại khá sâu sắc. Danh sách là một ý tưởng trừu tượng, còn các hộp bộ nhớ `size`, `items`, và `items[i]` là biểu diễn cụ thể của ý tưởng đó. Mọi thay đổi mà người dùng muốn thực hiện với danh sách thông qua các phép trừu tượng mà ta cung cấp (`addLast`, `removeLast`) đều phải được phản ánh thành các thay đổi cụ thể trên những hộp bộ nhớ này theo cách phù hợp với kỳ vọng của người dùng. Chính các bất biến sẽ chỉ đường cho ta biết các thay đổi đó nên trông như thế nào.


### Phụ lục: Thông tin bổ sung về mảng

Có ba ký hiệu hợp lệ để tạo mảng. Hãy thử chạy mã bên dưới và xem điều gì sẽ xảy ra. Nhấp vào [đây](http://pythontutor.com/iframe-embed.html#code=public%20class%20ArrayCreationDemo%20%7B%0A%20%20public%20static%20void%20main\(String%5B%5D%20args%29%20%7B%0A%20%20%20%20int%5B%5D%20x%3B%0A%20%20%20%20int%5B%5D%20y%3B%0A%20%20%20%20x%20%3D%20new%20int%5B3%5D%3B%0A%20%20%20%20y%20%3D%20new%20int%5B%5D%7B1,%202,%203,%204,%205%7D%3B%0A%20%20%20%20int%5B%5D%20z%20%3D%20%7B9,%2010,%2011,%2012,%2013%7D%3B%0A%09%7D%0A%7D\&codeDivHeight=400\&codeDivWidth=350\&cumulative=false\&curInstr=0\&heapPrimitives=false\&origin=opt-frontend.js\&py=java\&rawInputLstJSON=%5B%5D\&textReferences=false) để hiển thị tương tác.

* `x = new int[3];`
* `y = new int[]{1, 2, 3, 4, 5};`
* `int[] z = {9, 10, 11, 12, 13};`

Cả ba ký hiệu đều tạo thành một mảng. Ký hiệu đầu tiên, được sử dụng để tạo `x`, sẽ tạo một mảng có độ dài được chỉ định và điền vào mỗi ô bộ nhớ một giá trị mặc định. Trong trường hợp này, nó sẽ tạo một mảng có độ dài 3 và điền vào mỗi ô trong số 3 ô với giá trị `int` mặc định `0`.

Ký hiệu thứ hai, được sử dụng để tạo `y`, tạo một mảng có kích thước chính xác cần thiết để chứa các giá trị bắt đầu được chỉ định. Trong trường hợp này, nó tạo ra một mảng có độ dài 5, với năm phần tử cụ thể đó.

Ký hiệu thứ ba, được dùng để **vừa khai báo vừa tạo** `z`, có hành vi giống ký hiệu thứ hai. Khác biệt duy nhất là nó bỏ qua từ khóa `new`, và chỉ có thể được dùng cùng với một khai báo biến.

Không có ký hiệu nào trong số này tốt hơn bất kỳ ký hiệu nào khác.

#### Truy cập và sửa đổi mảng <a href="#array-access-and-modification" id="array-access-and-modification"></a>

Đoạn mã sau đây trình bày tất cả cú pháp chính mà chúng ta sẽ sử dụng để làm việc với mảng. Hãy thử xem qua mã bên dưới và đảm bảo bạn hiểu điều gì xảy ra khi mỗi dòng thực thi. Để làm như vậy, hãy nhấp vào [đây](https://goo.gl/bertuh) để hiển thị tương tác. Ngoại trừ dòng mã cuối cùng, chúng ta đã thấy tất cả cú pháp này trước đây.

```java
int[] z = null;
int[] x, y;

x = new int[]{1, 2, 3, 4, 5};
y = x;
x = new int[]{-1, 2, 5, 4, 99};
y = new int[3];
z = new int[0];
int xL = x.length;

String[] s = new String[6];
s[4] = "ketchup";
s[x[3] - x[1]] = "muffins";

int[] b = {9, 10, 11};
System.arraycopy(b, 0, x, 3, 2);
```

Dòng cuối cùng trình bày một cách để sao chép thông tin từ mảng này sang mảng khác. `System.arraycopy` có năm tham số:

* Mảng được sử dụng làm nguồn
* Bắt đầu từ đâu trong mảng nguồn
* Mảng được sử dụng làm đích
* Bắt đầu từ đâu trong mảng đích
* Có bao nhiêu mục để sao chép

Đối với những người thành thạo Python, `System.arraycopy(b, 0,x, 3, 2)` tương đương với `x[3:5] = b[0:2]` trong Python.

Một cách tiếp cận khác để sao chép mảng là sử dụng vòng lặp. `arraycopy` thường nhanh hơn vòng lặp và mang lại mã nhỏ gọn hơn. Nhược điểm duy nhất là `arraycopy` (được cho là) ​​khó đọc hơn. Lưu ý rằng mảng Java chỉ kiểm tra chỉ số (bounds) lúc chạy. Nghĩa là, đoạn mã sau biên dịch được nhưng sẽ lỗi khi chạy.

```java
int[] x = {9, 10, 11, 12, 13};
int[] y = new int[2];
int i = 0;
while (i < x.length) {
    y[i] = x[i];
    i += 1;
}
```

Hãy thử chạy mã này cục bộ trong tệp java hoặc trong [trình hiển thị](https://goo.gl/YHufJ6). Tên lỗi mà bạn gặp phải khi gặp sự cố là gì? Tên của lỗi có ý nghĩa không?

#### Mảng 2D trong Java <a href="#id-2d-arrays-in-java" id="id-2d-arrays-in-java"></a>

Cái mà người ta có thể gọi là mảng 2D trong Java thực ra chỉ là một mảng các mảng. Chúng tuân theo các quy tắc tương tự đối với các đối tượng mà chúng ta đã học nhưng hãy xem lại chúng để đảm bảo rằng chúng ta hiểu cách chúng hoạt động.

Cú pháp cho mảng có thể hơi khó hiểu. Hãy xem xét đoạn mã `int[][] bamboozle = new int[4][]`. Điều này tạo ra một mảng các mảng số nguyên tên là `bamboozle`. Cụ thể, điều này tạo ra chính xác bốn hộp bộ nhớ, mỗi hộp có thể trỏ đến một mảng số nguyên (có độ dài không xác định).

Hãy thử chạy mã bên dưới từng dòng một và xem kết quả có khớp với trực giác của bạn không. Để hiển thị trực quan mang tính tương tác, hãy nhấp vào [đây](http://goo.gl/VS4cOK).

```java
int[][] pascalsTriangle;
pascalsTriangle = new int[4][];
int[] rowZero = pascalsTriangle[0];

pascalsTriangle[0] = new int[]{1};
pascalsTriangle[1] = new int[]{1, 1};
pascalsTriangle[2] = new int[]{1, 2, 1};
pascalsTriangle[3] = new int[]{1, 3, 3, 1};
int[] rowTwo = pascalsTriangle[2];
rowTwo[1] = -5;

int[][] matrix;
matrix = new int[4][];
matrix = new int[4][4];

int[][] pascalAgain = new int[][]{{1}, {1, 1},
                                 {1, 2, 1}, {1, 3, 3, 1}};
```

**Bài tập 2.4.1:** Sau khi chạy đoạn mã bên dưới, giá trị của x\[0]\[0] và w\[0]\[0] sẽ là bao nhiêu? Kiểm tra công việc của bạn bằng cách nhấp vào [đây](http://goo.gl/fCZ9Dr).

```java
int[][] x = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};

int[][] z = new int[3][];
z[0] = x[0];
z[1] = x[1];
z[2] = x[2];
z[0][0] = -z[0][0];

int[][] w = new int[3][3];
System.arraycopy(x[0], 0, w[0], 0, 3);
System.arraycopy(x[1], 0, w[1], 0, 3);
System.arraycopy(x[2], 0, w[2], 0, 3);
w[0][0] = -w[0][0];
```

#### Mảng so với lớp <a href="#arrays-vs-classes" id="arrays-vs-classes"></a>

Cả mảng và lớp đều có thể được sử dụng để sắp xếp một loạt các hộp bộ nhớ. Trong cả hai trường hợp, số lượng hộp bộ nhớ là cố định, tức là không thể thay đổi độ dài của mảng, giống như không thể thêm hoặc xóa các trường lớp.

Sự khác biệt chính giữa các hộp bộ nhớ trong mảng và lớp:

* Các hộp mảng được đánh số và truy cập bằng ký hiệu `[]`, còn các hộp lớp được đặt tên và truy cập bằng ký hiệu dấu chấm.
* Tất cả các hộp mảng phải cùng loại. Hộp lớp có thể có nhiều loại khác nhau.

Một tác động đặc biệt đáng chú ý của những khác biệt này là ký hiệu `[]` cho phép chúng ta chỉ định chỉ mục nào chúng ta muốn trong thời gian chạy. Ví dụ, hãy xem xét đoạn mã dưới đây:

```java
int indexOfInterest = askUserForInteger();
int[] x = {100, 101, 102, 103};
int k = x[indexOfInterest];
System.out.println(k);
```

Nếu ta chạy mã này, ta có thể nhận được kết quả như sau:

```
$ javac arrayDemo
$ java arrayDemo
What index do you want? 2
102
```

Ngược lại, việc chọn trường của một lớp không phải việc ta làm động lúc chạy theo kiểu mảng. Ví dụ, hãy xem đoạn mã dưới đây:

```java
String fieldOfInterest = "mass";
Planet p = new Planet(6e24, "earth");
double mass = p[fieldOfInterest];
```

Nếu ta thử biên dịch đoạn này, ta sẽ gặp lỗi cú pháp.

```
$ javac classDemo
FieldDemo.java:5: error: array required, but Planet found
        double mass = earth[fieldOfInterest];        
                               ^
```

Vấn đề tương tự cũng xảy ra nếu chúng ta cố gắng sử dụng ký hiệu dấu chấm:

```java
String fieldOfInterest = "mass";
Planet p = new Planet(6e24, "earth");
double mass = p.fieldOfInterest;
```

Biên dịch, ta sẽ nhận được:

```
$ javac classDemo
FieldDemo.java:5: error: cannot find symbol
        double mass = earth.fieldOfInterest;        
                           ^
  symbol:   variable fieldOfInterest
  location: variable earth of type Planet
```

Đây không phải hạn chế mà bạn thường gặp, nhưng đáng biết vì lý do sư phạm. Về mặt kỹ thuật, có cách chọn trường lúc chạy gọi là _reflection_, nhưng đó được xem là lối lập trình rất tệ trong mã ứng dụng thông thường. Bạn có thể đọc thêm về reflection [đây](https://docs.oracle.com/javase/tutorial/reflect/member/fieldValues.html). **Đừng dùng reflection trong bất kỳ bài tập 61B nào**; khóa học cũng không dạy sâu chủ đề này.

Nói chung, ngôn ngữ lập trình được thiết kế một phần để giới hạn “cánh cửa” người lập trình có thể mở, giúp mã dễ đọc hơn. Bằng cách gom các thao tác kiểu đó vào API Reflection riêng, các chương trình Java điển hình dễ đọc và lý giải hơn.

#### Phụ lục: Mảng Java so với các ngôn ngữ khác <a href="#appendix-java-arrays-vs-other-Language" id="appendix-java-arrays-vs-other-lingutions"></a>

So với mảng trong các ngôn ngữ khác, mảng Java:

* Không có cú pháp đặc biệt để "cắt" (chẳng hạn như trong Python).
* Không thể thu nhỏ hoặc mở rộng (chẳng hạn như trong Ruby).
* Không có phương thức thành viên (chẳng hạn như trong Javascript).
* Chỉ được chứa các giá trị cùng loại (không giống như Python).
