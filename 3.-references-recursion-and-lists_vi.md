# 3. Tham chiếu, đệ quy và danh sách

### Danh sách <a href="#lists" id="lists"></a>

Bạn chắc chắn đã từng sử dụng cấu trúc dữ liệu danh sách tại một thời điểm nào đó trong quá khứ. Ví dụ: trong Python:

```python
L = [3, 5, 6]
L.append(7)
```

Mặc dù Java có sẵn kiểu `List`, hiện tại chúng ta sẽ tạm thời không dùng đến nó. Trong chương này, chúng ta sẽ tự xây dựng danh sách của riêng mình từ đầu, đồng thời học một số đặc điểm quan trọng của Java.

#### Bí ẩn của Hải mã <a href="#the-mystery-of-the-walrus" id="the-mystery-of-the-walrus"></a>

Để bắt đầu hành trình này, trước hết ta sẽ suy ngẫm về Bí ẩn sâu sắc của Hải mã.

Hãy thử đoán điều gì xảy ra khi ta chạy đoạn mã dưới đây. Việc thay đổi `b` có ảnh hưởng đến `a` không? Gợi ý: nếu bạn quen với Python, Java cũng có hành vi tương tự.

```java
Walrus a = new Walrus(1000, 8.3);
Walrus b;
b = a;
b.weight = 5;
System.out.println(a);
System.out.println(b);
```

Bây giờ hãy thử dự đoán điều gì sẽ xảy ra khi chúng ta chạy đoạn mã bên dưới. Việc thay đổi x có ảnh hưởng đến y không?

```java
int x = 5;
int y;
y = x;
x = 2;
System.out.println("x is: " + x);
System.out.println("y is: " + y);
```

Câu trả lời có thể được tìm thấy [đây](http://cscircles.cemc.uwaterloo.ca/java_visualize/#code=public+class+PollQuestions+%7B%0A+++public+static+void+main%28String%5B%5D+args%29+%7B%0A++++++Walrus+a+%3D+new+Walrus%281000,+8.3%29%3B%0A++++++Walrus+b%3B%0A++++++b+%3D+a%3B%0A++++++b.weight+%3D+5%3B%0A++++++System.out.println%28a%29%3B%0A++++++System.out.println%28b%29%3B++++++%0A%0A++++++int+x+%3D+5%3B%0A++++++int+y%3B%0A++++++y+%3D+x%3B%0A++++++x+%3D+2%3B%0A++++++System.out.println%28%22x+is%3A+%22+%2B+x%29%3B%0A++++++System.out.println%28%22y+is%3A+%22+%2B+y%29%3B++++++%0A+++%7D%0A+++%0A+++public+static+class+Walrus+%7B%0A++++++public+int+weight%3B%0A++++++public+double+tuskSize%3B%0A++++++%0A++++++public+Walrus%28int+w,+double+ts%29+%7B%0A+++++++++weight+%3D+w%3B%0A+++++++++tuskSize+%3D+ts%3B%0A++++++%7D%0A%0A++++++public+String+toString%28%29+%7B%0A+++++++++return+String.format%28%22weight%3A+%25d,+tusk+size%3A+%25.2f%22,+weight,+tuskSize%29%3B%0A++++++%7D%0A+++%7D%0A%7D\&mode=edit).

Mặc dù tinh tế nhưng những ý tưởng chính làm nền tảng cho Bí ẩn của Hải mã sẽ cực kỳ quan trọng đối với hiệu quả của cấu trúc dữ liệu mà chúng ta sẽ triển khai trong khóa học này và sự hiểu biết sâu sắc về vấn đề này cũng sẽ dẫn đến mã an toàn hơn, đáng tin cậy hơn.

#### Số bit <a href="#bits" id="bits"></a>

Tất cả thông tin trong máy tính của bạn được lưu trữ trong *bộ nhớ* dưới dạng một chuỗi bit 0 và 1. Một số ví dụ:

* 72 thường được lưu dưới dạng 01001000
* 205,75 thường được lưu trữ dưới dạng 01000011 01001101 11000000 00000000
* Chữ H thường được lưu là 01001000 (giống 72)
* Giá trị thực thường được lưu trữ dưới dạng 00000001

Trong khóa học này, chúng ta sẽ không dành nhiều thời gian để nói về các biểu diễn nhị phân cụ thể, ví dụ: tại sao trên trái đất 205,75 được lưu trữ dưới dạng chuỗi 32 bit dường như ngẫu nhiên ở trên. Tìm hiểu các cách trình bày cụ thể là chủ đề của [CS61C](http://www-inst.eecs.berkeley.edu/~cs61c/), khóa học tiếp theo của 61B.

Mặc dù chúng ta sẽ không học ngôn ngữ nhị phân, nhưng thật tốt khi biết rằng đây chính là điều đang diễn ra.

Một quan sát thú vị là cả 72 và H đều được lưu dưới dạng 01001000. Điều này đặt ra câu hỏi: làm thế nào một đoạn mã Java biết cách diễn giải 01001000?

Câu trả lời là thông qua các loại! Ví dụ, hãy xem xét đoạn mã dưới đây:

```java
char c = 'H';
int x = c;
System.out.println(c);
System.out.println(x);
```

Nếu chúng tôi chạy mã này, chúng tôi nhận được:

```
H
72
```

Trong trường hợp này, cả hai biến x và c đều chứa các bit giống nhau (à, gần như...), nhưng trình thông dịch Java xử lý chúng khác nhau khi được in.

Trong Java, có 8 kiểu nguyên thủy: byte, short, int, long, float, double, boolean và char. Mỗi loại có các thuộc tính khác nhau mà chúng ta sẽ thảo luận trong suốt khóa học, ngoại trừ short và float mà bạn có thể sẽ không bao giờ sử dụng.

#### Khai báo một biến (Đơn giản hóa) <a href="#declaring-a-variable-simplified" id="declaring-a-variable-simplified"></a>

Bạn có thể hình dung máy tính của mình chứa vô số bit bộ nhớ để lưu trữ thông tin, mỗi bit có một địa chỉ duy nhất. Máy tính hiện đại có hàng tỷ bit như vậy.

Khi bạn khai báo một biến thuộc một loại nhất định, Java sẽ tìm thấy một khối liền kề có đủ số bit chính xác để chứa một thứ thuộc loại đó. Ví dụ: nếu bạn khai báo một int, bạn sẽ nhận được một khối 32 bit. Nếu bạn khai báo một byte, bạn sẽ nhận được một khối 8 bit. Mỗi kiểu dữ liệu trong Java chứa một số bit khác nhau. Con số chính xác không quá quan trọng đối với chúng tôi trong lớp học này.

Để có một phép ẩn dụ thuận tiện, chúng tôi sẽ gọi một trong những khối này là "hộp" bit.

Ngoài việc dành bộ nhớ, trình thông dịch Java còn tạo một mục trong bảng nội bộ để ánh xạ từng tên biến tới vị trí của bit đầu tiên trong hộp.

Ví dụ: nếu bạn khai báo `int x` và `double y` thì Java có thể quyết định sử dụng các bit từ 352 đến 384 của bộ nhớ máy tính của bạn để lưu trữ x và các bit từ 20800 đến 20864 để lưu trữ y. Sau đó, trình thông dịch sẽ ghi lại rằng int x bắt đầu ở bit 352 và y bắt đầu ở bit 20800. Ví dụ: sau khi thực thi mã:

```java
int x;
double y;
```

Chúng ta sẽ có các hộp có kích thước lần lượt là 32 và 64, như trong hình bên dưới:

![x\_and\_y\_empty\_bitwise](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/x_and_y_empty_bitwise.png)

Ngôn ngữ Java không cung cấp cách nào để bạn biết vị trí của hộp, ví dụ: bằng cách nào đó bạn không thể phát hiện ra rằng x ở vị trí 352. Nói cách khác, địa chỉ bộ nhớ chính xác nằm dưới mức trừu tượng mà chúng ta có thể truy cập được trong Java. Điều này không giống như các ngôn ngữ như C nơi bạn có thể yêu cầu ngôn ngữ đó cung cấp địa chỉ chính xác của một phần dữ liệu. Vì lý do này, tôi đã bỏ qua các địa chỉ trong hình trên.

Tính năng này của Java là một sự đánh đổi! Việc ẩn các vị trí bộ nhớ khỏi lập trình viên sẽ mang lại cho bạn ít quyền kiểm soát hơn, điều này ngăn cản bạn thực hiện một số [các loại tối ưu hóa](http://www.informit.com/articles/article.aspx?p=2246428\&seqNum=5) nhất định. Tuy nhiên, nó cũng tránh được [lớp lớn các lỗi lập trình rất phức tạp](http://www.informit.com/articles/article.aspx?p=2246428\&seqNum=1). Trong kỷ nguyên hiện đại của điện toán chi phí rất thấp, sự đánh đổi này thường rất xứng đáng. Như Donald Knuth thông thái đã từng nói: “Chúng ta nên quên đi những hiệu quả nhỏ, khoảng 97% trường hợp: tối ưu hóa sớm là gốc rễ của mọi tội lỗi”.

Tương tự, bạn không có quyền kiểm soát trực tiếp nhịp tim của mình. Mặc dù điều này hạn chế khả năng tối ưu hóa của bạn trong một số trường hợp nhất định nhưng nó cũng tránh được khả năng mắc các lỗi ngu ngốc như vô tình tắt tính năng này.

Java không ghi bất cứ thứ gì vào hộp dành riêng khi một biến được khai báo. Nói cách khác, không có giá trị mặc định. Kết quả là, trình biên dịch Java ngăn không cho bạn sử dụng một biến cho đến khi hộp đã được lấp đầy các bit bằng toán tử `=`. Vì lý do này, tôi đã tránh hiển thị bất kỳ bit nào trong các hộp ở hình trên.

Khi bạn gán giá trị cho hộp bộ nhớ, nó sẽ chứa đầy các bit bạn chỉ định. Ví dụ: nếu chúng ta thực thi các dòng:

```java
x = -1431195969;
y = 567213.112;
```

Sau đó, các hộp bộ nhớ ở trên được điền vào như hiển thị bên dưới, cái mà tôi gọi là **ký hiệu hộp**.

![x\_and\_y\_empty\_filled.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/x_and_y_empty_filled.png)

Các bit trên cùng đại diện cho -1431195969 và các bit dưới cùng đại diện cho 567213.112. Tại sao các chuỗi bit cụ thể này đại diện cho hai số này không quan trọng và là chủ đề được đề cập trong CS61C. Tuy nhiên, nếu bạn tò mò, hãy xem [biểu diễn số nguyên](https://en.wikipedia.org/wiki/Two's_complement) và [biểu diễn kép](https://en.wikipedia.org/wiki/IEEE_floating_point) trên wikipedia.

Lưu ý: Việc cấp phát bộ nhớ thực sự phức tạp hơn một chút so với mô tả ở đây và là chủ đề của CS 61C. Tuy nhiên, mô hình này đủ gần với thực tế cho mục đích của chúng ta trong 61B.

**Ký hiệu hộp đơn giản**

Mặc dù ký hiệu hộp mà chúng ta sử dụng ở phần trước rất hữu ích để hiểu gần đúng những gì đang diễn ra nhưng nó không hữu ích cho các mục đích thực tế vì chúng ta không biết cách diễn giải các bit nhị phân.

Vì vậy, thay vì ghi nội dung hộp bộ nhớ ở dạng nhị phân, chúng ta sẽ viết chúng bằng các ký hiệu mà con người có thể đọc được. Chúng tôi sẽ làm điều này trong suốt phần còn lại của khóa học. Ví dụ: sau khi thực hiện:

```java
int x;
double y;
x = -1431195969;
y = 567213.112;
```

Chúng ta có thể biểu diễn môi trường chương trình bằng cách sử dụng cái mà tôi gọi là **ký hiệu hộp đơn giản**, được hiển thị bên dưới:

![x\_and\_y\_simplified\_box\_notation.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/x_and_y_simplified_box_notation.png)

#### Quy tắc vàng của sự bình đẳng (GRoE) <a href="#the-golden-rule-of-equals-groe" id="the-golden-rule-of-equals-groe"></a>

Bây giờ được trang bị ký hiệu hộp đơn giản hóa, cuối cùng chúng ta cũng có thể bắt đầu giải quyết Bí ẩn về Hải mã.

Hóa ra Bí ẩn của chúng tôi có một giải pháp đơn giản: Khi bạn viết `y = x`, bạn đang yêu cầu trình thông dịch Java sao chép các bit từ x vào y. Quy tắc Vàng Bình đẳng (GRoE) này là gốc rễ của mọi sự thật khi hiểu về Bí ẩn Hải mã của chúng ta.

```java
int x = 5;
int y;
y = x;
x = 2;
System.out.println("x is: " + x);
System.out.println("y is: " + y);
```

Ý tưởng đơn giản về việc sao chép các bit này đúng với BẤT KỲ phép gán nào sử dụng `=` trong Java. Để xem tính năng này hoạt động, hãy nhấp vào [liên kết này](http://cscircles.cemc.uwaterloo.ca/java_visualize/#code=public+class+PollQuestions+%7B%0A+++public+static+void+main\(String%5B%5D+args%29+%7B%0A++++++int+x+%3D+5%3B%0A++++++int+y%3B%0A++++++y+%3D+x%3B%0A++++++x+%3D+2%3B%0A++++++System.out.println\(%22x+is%3A+%22+%2B+x%29%3B%0A++++++System.out.println\(%22y+is%3A+%22+%2B+y%29%3B++++++%0A+++%7D%0A%7D\&mode=display\&curInstr=0).

#### Các loại tham chiếu <a href="#reference-types" id="reference-types"></a>

Ở trên ta đã nói có 8 kiểu nguyên thủy: byte, short, int, long, float, double, boolean, char. Mọi thứ khác, bao gồm cả mảng, không phải là kiểu nguyên thủy mà là `kiểu tham chiếu`.

**Khởi tạo đối tượng**

Khi chúng ta khởi tạo một đối tượng bằng `new` (ví dụ: Dog, Walrus, Planet), trước hết Java sẽ cấp phát một hộp cho mỗi biến thực thể của lớp và điền chúng bằng giá trị mặc định. Sau đó, hàm tạo thường (nhưng không phải luôn luôn) sẽ gán thêm giá trị khác cho từng ô.

Ví dụ: nếu lớp Walrus của chúng tôi là:

```java
public static class Walrus {
    public int weight;
    public double tuskSize;

    public Walrus(int w, double ts) {
          weight = w;
          tuskSize = ts;
    }
}
```

Và chúng ta tạo một Walrus bằng cách sử dụng `new Walrus(1000, 8.3);`, sau đó chúng ta thu được một Walrus bao gồm hai hộp 32 và 64 bit tương ứng:

![anonymous\_walrus.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/anonymous_walrus.png)

Trong quá trình triển khai thực sự của ngôn ngữ lập trình Java, thực sự có một số chi phí bổ sung cho bất kỳ đối tượng nào, do đó, Walrus cần nhiều hơn 96 bit một chút. Tuy nhiên, vì mục đích của chúng tôi, chúng tôi sẽ bỏ qua chi phí đó vì chúng tôi sẽ không bao giờ tương tác trực tiếp với nó.

Walrus mà chúng tôi đã tạo là ẩn danh, theo nghĩa là nó đã được tạo nhưng nó không được lưu trữ trong bất kỳ biến nào. Bây giờ chúng ta hãy chuyển sang các biến lưu trữ các đối tượng.

**Khai báo biến tham chiếu**

Khi chúng ta khai báo một biến thuộc bất kỳ kiểu tham chiếu nào (Walrus, Dog, Planet, mảng, v.v.), Java sẽ phân bổ một hộp gồm 64 bit, bất kể loại đối tượng nào.

Thoạt nhìn, điều này có vẻ dẫn đến Nghịch lý Walrus. Walrus của chúng tôi ở phần trước yêu cầu nhiều hơn 64 bit để lưu trữ. Hơn nữa, có vẻ kỳ lạ là bất kể loại đối tượng nào, chúng ta chỉ có 64 bit để lưu trữ nó.

Tuy nhiên, vấn đề này có thể được giải quyết dễ dàng bằng đoạn thông tin sau: hộp 64 bit không chứa dữ liệu về hải mã mà thay vào đó là địa chỉ của hải mã trong bộ nhớ.

Ví dụ: giả sử chúng ta gọi:

```java
Walrus someWalrus;
someWalrus = new Walrus(1000, 8.3);
```

Dòng đầu tiên tạo ra một hộp 64 bit. Dòng thứ hai tạo một Walrus mới và địa chỉ được trả về bởi toán tử `new`. Các bit này sau đó được sao chép vào hộp `someWalrus` theo GRoE.

Nếu chúng ta tưởng tượng trọng lượng Walrus của chúng ta được lưu trữ bắt đầu từ bit `5051956592385990207` của bộ nhớ và tuskSize bắt đầu ở bit `5051956592385990239`, thì chúng ta có thể lưu trữ `5051956592385990207` trong biến Walrus. Trong hệ nhị phân, `5051956592385990207` được biểu thị bằng 64 bit `0100011000011100001001111100000100011101110111000001111000111111`, cho chúng ta ký hiệu trong hộp:

![someWalrus\_bit\_notation.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/someWalrus_bit_notation.png)

Chúng ta cũng có thể gán giá trị đặc biệt `null` cho một biến tham chiếu, tương ứng với tất cả các số 0.

![someWalrus\_bit\_notation\_null.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/someWalrus_bit_notation_null.png)

**Ký hiệu hộp và con trỏ**

Cũng giống như trước đây, thật khó để diễn giải một loạt bit bên trong một biến tham chiếu, vì vậy chúng ta sẽ tạo một ký hiệu hộp đơn giản cho biến tham chiếu như sau:

* Nếu một địa chỉ toàn là số 0, chúng tôi sẽ biểu thị nó bằng null.
* Địa chỉ khác 0 sẽ được biểu thị bằng **mũi tên** chỉ vào một đối tượng khởi tạo.

Điều này đôi khi còn được gọi là ký hiệu "hộp và con trỏ".

Đối với các ví dụ từ phần trước, chúng ta sẽ có:

![someWalrus\_simplified\_bit\_notation.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/someWalrus_simplified_bit_notation.png)

![someWalrus\_simplified\_bit\_notation\_null.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/someWalrus_simplified_bit_notation_null.png)

**Giải đáp bí ẩn về con hải mã**

Bây giờ chúng ta cuối cùng đã sẵn sàng giải đáp một cách đầy đủ và trọn vẹn Bí ẩn về Hải mã.

```java
Walrus a = new Walrus(1000, 8.3);
Walrus b;
b = a;
```

Sau khi dòng đầu tiên được thực thi, chúng ta có:

![mystery\_of\_the\_walrus\_resolved\_step1.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/mystery_of_the_walrus_resolved_step1.png)

Sau khi dòng thứ hai được thực thi, chúng ta có:

![mystery\_of\_the\_walrus\_resolved\_step2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/mystery_of_the_walrus_resolved_step2.png)

Lưu ý rằng ở trên, b không được xác định, không phải là null.

Theo GRoE, dòng cuối cùng chỉ đơn giản sao chép các bit trong hộp `a` sang hộp `b`. Hoặc theo cách ẩn dụ trực quan của chúng ta, điều này có nghĩa là b sẽ sao chép chính xác mũi tên trong a và bây giờ hiển thị một mũi tên chỉ vào cùng một đối tượng.

![mystery\_of\_the\_walrus\_resolved\_step3.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/mystery_of_the_walrus_resolved_step3.png)

Và thế là xong. Không có gì phức tạp hơn thế này.

#### Truyền tham số <a href="#parameter-passing" id="parameter-passing"></a>

Khi bạn truyền tham số cho một hàm, bạn cũng chỉ đơn giản sao chép các bit. Nói cách khác, GRoE cũng áp dụng cho việc truyền tham số. Sao chép các bit thường được gọi là "truyền theo giá trị". Trong Java, chúng tôi **luôn** truyền theo giá trị.

Ví dụ, hãy xem xét hàm dưới đây:

```java
public static double average(double a, double b) {
    return (a + b) / 2;
}
```

Giả sử chúng ta gọi hàm này như dưới đây:

```java
public static void main(String[] args) {
    double x = 5.5;
    double y = 10.5;
    double avg = average(x, y);
}
```

Sau khi thực thi hai dòng đầu tiên của hàm này, phương thức chính sẽ có hai hộp có nhãn `x` và `y` chứa các giá trị hiển thị bên dưới:

![main\_x\_y.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/main_x_y.png)

Khi hàm được gọi, hàm `average` có **phạm vi riêng** của nó, với hai hộp mới mang nhãn `a` và `b`, và các bit đơn giản chỉ được _sao chép_ vào đó. Việc sao chép các bit này chính là điều ta muốn nói khi gọi là "truyền theo giá trị".

![average\_a\_b.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig21/average_a_b.png)

Nếu hàm `average` thay đổi `a`, thì `x` trong `main` sẽ không đổi, vì GRoE cho ta biết rằng việc đó chỉ đơn giản là điền các bit mới vào hộp mang nhãn `a`.

**Kiểm tra sự hiểu biết của bạn**

**Bài tập 2.1.1**: Giả sử chúng ta có đoạn mã dưới đây:

```java
public class PassByValueFigure {
    public static void main(String[] args) {
           Walrus walrus = new Walrus(3500, 10.5);
           int x = 9;

           doStuff(walrus, x);
           System.out.println(walrus);
           System.out.println(x);
    }

    public static void doStuff(Walrus W, int x) {
           W.weight = W.weight - 100;
           x = x - 5;
    }
}
```

Lệnh gọi `doStuff` có ảnh hưởng đến hải mã và/hoặc x không? Gợi ý: Chúng ta chỉ cần biết GRoE để giải bài toán này.

#### Khởi tạo mảng <a href="#instantiation-of-arrays" id="instantiation-of-arrays"></a>

Như đã đề cập ở trên, các biến lưu trữ mảng là các biến tham chiếu giống như bất kỳ biến nào khác. Ví dụ, hãy xem xét các khai báo dưới đây:

```java
int[] x;
Planet[] planets;
```

Cả hai khai báo này đều tạo ra các hộp bộ nhớ 64 bit. `x` chỉ có thể giữ địa chỉ của mảng `int` và `planets` chỉ có thể giữ địa chỉ của mảng `Planet`.

Việc khởi tạo một mảng rất giống với việc khởi tạo một đối tượng. Ví dụ: nếu chúng ta tạo một mảng số nguyên có kích thước 5 như dưới đây:

```
x = new int[]{0, 1, 2, 95, 4};
```

Sau đó, từ khóa `new` tạo 5 hộp, mỗi hộp 32 bit và trả về địa chỉ của đối tượng tổng thể để gán cho x.

Các đối tượng có thể bị mất nếu bạn mất các bit tương ứng với địa chỉ. Ví dụ: nếu bản sao duy nhất địa chỉ của một Walrus cụ thể được lưu trữ trong `x`, thì `x = null` sẽ khiến bạn mất vĩnh viễn Walrus này. Đây không hẳn là một điều xấu, vì bạn thường quyết định rằng mình đã hoàn thành xong một đối tượng và do đó, việc vứt bỏ tham chiếu đó là an toàn. Chúng ta sẽ thấy điều này khi xây dựng danh sách ở phần sau của chương này.

**Luật Futon bị gãy**

Bạn có thể tự hỏi tại sao chúng tôi lại dành nhiều thời gian và không gian như vậy để đề cập đến những điều tưởng chừng như tầm thường. Điều này có lẽ đặc biệt đúng nếu bạn đã có kinh nghiệm về Java trước đó. Lý do là học sinh rất dễ hiểu nửa vời về vấn đề này, cho phép họ viết mã nhưng không thực sự hiểu chuyện gì đang xảy ra.

Mặc dù điều này có thể tốt trong ngắn hạn, nhưng về lâu dài, việc giải quyết vấn đề mà không hiểu biết đầy đủ có thể khiến bạn thất bại sau này. Có một bài đăng trên blog về cái gọi là [Luật Futon bị hỏng](https://mathwithbaddrawings.com/2015/04/08/the-math-ceiling-wheres-your-cognitive-breaking-point/) mà bạn có thể thấy thú vị.

#### == so với Arrays.equals <a href="#instantiation-of-arrays" id="instantiation-of-arrays"></a>

Tương tự như với `=`, ta có thể nghĩ về toán tử `==` theo góc nhìn bit. Mỗi khi viết `x == y`, ta đang yêu cầu Java so sánh chính các bit nằm trong hai hộp bộ nhớ `x` và `y`.

Ví dụ: giả sử chúng ta có mã:

```java
int[] x = new int[]{0, 1, 2, 95, 4};
int[] y = new int[]{0, 1, 2, 95, 4};
System.out.println(x == y); #false
```

Mã này sẽ in sai, vì `x` và `y` mỗi mảng chứa địa chỉ 64 bit của hai mảng khác nhau trong bộ nhớ, mặc dù hai mảng tình cờ chứa cùng một thông tin. Nếu muốn so sánh hai nội dung của hai mảng, thay vào đó chúng ta có thể sử dụng Arrays.equals, ví dụ:

```java
int[] x = new int[]{0, 1, 2, 95, 4};
int[] y = new int[]{0, 1, 2, 95, 4};
System.out.println(Arrays.equals(x, y)); #true
```

#### IntLists <a href="#intlists" id="intlists"></a>

Bây giờ chúng ta đã thực sự hiểu Bí ẩn của Hải mã, chúng ta đã sẵn sàng xây dựng lớp danh sách của riêng mình.

Hóa ra việc thực hiện một danh sách rất cơ bản là không đáng kể, như minh họa bên dưới:

```java
public class IntList {
    public int first;
    public IntList rest;        

    public IntList(int f, IntList r) {
        first = f;
        rest = r;
    }
}
```

Bạn có thể nhớ một cái gì đó tương tự như thế này từ 61a được gọi là "Danh sách liên kết".

Một danh sách như vậy là xấu để sử dụng. Ví dụ: nếu muốn tạo danh sách các số 5, 10 và 15, chúng ta có thể thực hiện:

```java
IntList L = new IntList(5, null);
L.rest = new IntList(10, null);
L.rest.rest = new IntList(15, null);
```

Ngoài ra, chúng ta có thể xây dựng ngược danh sách của mình, mang lại mã đẹp hơn một chút nhưng khó hiểu hơn:

```java
IntList L = new IntList(15, null);
L = new IntList(10, L);
L = new IntList(5, L);
```

Mặc dù về nguyên tắc bạn có thể sử dụng IntList để lưu trữ bất kỳ danh sách số nguyên nào, nhưng mã kết quả sẽ khá xấu và dễ bị lỗi. Chúng ta sẽ áp dụng chiến lược lập trình hướng đối tượng thông thường là thêm các phương thức trợ giúp vào lớp của chúng ta để thực hiện các tác vụ cơ bản.

**`size` và `iterativeSize`**

Chúng tôi muốn thêm một phương thức `size` vào lớp `IntList` để nếu bạn gọi `L.size()`, bạn sẽ lấy lại số lượng mục trong `L`.

Hãy cân nhắc viết phương thức `size` và `iterativeSize` trước khi đọc phần còn lại của chương này. `size` nên sử dụng đệ quy và `iterativeSize` không nên sử dụng. Có thể bạn sẽ học được nhiều hơn bằng cách tự mình thử trước khi xem cách tôi làm. Hai video minh họa trực tiếp cách triển khai các phương thức này.

Phương thức `size` của tôi như dưới đây:

```java
/** Return the size of the list using... recursion! */
public int size() {
    if (rest == null) {
        return 1;
    }
    return 1 + this.rest.size();
}
```

Điều quan trọng cần nhớ về mã đệ quy là bạn cần một trường hợp cơ sở. Trong tình huống này, trường hợp cơ bản hợp lý nhất là phần còn lại là `null`, dẫn đến danh sách có kích thước 1.

Bài tập: Bạn có thể thắc mắc tại sao chúng ta không làm điều gì đó như `if (this == null) return 0;`. Tại sao điều này không hoạt động?

Trả lời: Hãy nghĩ xem điều gì sẽ xảy ra khi bạn gọi size. Bạn đang gọi nó trên một đối tượng, ví dụ L.size(). Nếu L là null thì bạn sẽ gặp lỗi NullPointer!

Phương thức `iterativeSize` của tôi như dưới đây. Tôi khuyên bạn rằng khi viết mã cấu trúc dữ liệu lặp, bạn nên sử dụng tên `p` để nhắc nhở bản thân rằng biến đang chứa một con trỏ. Bạn cần con trỏ đó vì bạn không thể gán lại "cái này" trong Java. Phần tiếp theo trong [bài đăng tràn ngăn xếp này](https://stackoverflow.com/questions/23021377/reassign-this-in-java-class) đưa ra lời giải thích ngắn gọn về lý do.

```java
/** Return the size of the list using no recursion! */
public int iterativeSize() {
    IntList p = this;
    int totalSize = 0;
    while (p != null) {
        totalSize += 1;
        p = p.rest;
    }
    return totalSize;
}
```

**`get`**

Mặc dù phương thức `size` cho phép chúng ta lấy kích thước của danh sách, nhưng chúng ta không có cách nào dễ dàng để lấy phần tử thứ i của danh sách.

Bài tập: Viết phương thức `get(int i)` trả về mục thứ i của danh sách. Ví dụ: nếu `L` là 5 -> 10 -> 15, thì `L.get(0)` sẽ trả về 5, `L.get(1)` sẽ trả về 10 và `L.get(2)` sẽ trả về 15. Mã của bạn hoạt động như thế nào đối với `i` không hợp lệ, quá lớn hoặc quá nhỏ.

Để biết giải pháp, hãy xem video bài giảng ở trên hoặc kho lưu trữ Mã bài giảng.

Lưu ý rằng phương thức ta đã viết có độ phức tạp thời gian tuyến tính: nếu bạn có một danh sách dài 1.000.000 mục thì việc lấy mục cuối cùng sẽ mất nhiều thời gian hơn so với khi chúng ta có một danh sách nhỏ. Chúng ta sẽ thấy một cách khác để triển khai danh sách nhằm tránh vấn đề này trong bài giảng sau.
