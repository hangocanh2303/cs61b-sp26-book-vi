# 7. ArrayList thay đổi kích thước

#### Mảng thay đổi kích thước kiểu thô sơ <a href="#naive-resizing-arrays" id="naive-resizing-arrays"></a>

**Bài tập tùy chọn 2.5.3:** Giả sử chúng ta có một AList ở trạng thái như trong hình bên dưới. Điều gì sẽ xảy ra nếu chúng ta gọi `addLast(11)`? Chúng ta nên làm gì về vấn đề này?

![dllist\_circular\_sentinel\_size\_2.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig25/full_naive_alist.png)

Câu trả lời trong Java là ta chỉ cần tạo một mảng mới đủ lớn để chứa dữ liệu mới. Ví dụ, ta có thể hình dung việc thêm phần tử mới như sau:

```java
int[] a = new int[size + 1];
System.arraycopy(items, 0, a, 0, size);
a[size] = 11;
items = a;
size = size + 1;
```

Quá trình tạo một mảng mới rồi sao chép các phần tử sang thường được gọi là "thay đổi kích thước". Tên gọi này hơi không chính xác, vì mảng cũ không thực sự đổi kích thước; ta chỉ đang tạo ra một mảng **mới** có kích thước lớn hơn.

**Bài tập 2.5.4:** Thử triển khai phương thức `addLast(int i)` để làm việc với việc thay đổi kích thước mảng.

#### Phân tích mảng thay đổi kích thước kiểu thô sơ <a href="#analyzing-the-naive-resizing-array" id="analyzing-the-naive-resizing-array"></a>

Cách tiếp cận mà ta thử ở phần trước có hiệu năng rất tệ. Nếu chạy một thí nghiệm tính toán đơn giản, trong đó gọi `addLast` 100.000 lần, ta sẽ thấy `SLList` hoàn thành nhanh đến mức gần như không đo được thời gian, trong khi danh sách dựa trên mảng lại mất vài giây.

Để hiểu lý do tại sao, hãy xem xét bài tập sau:

**Bài tập 2.5.5:** Giả sử chúng ta có một mảng có kích thước 100. Nếu chúng ta gọi `insertBack` hai lần, chúng ta sẽ cần tạo và điền tổng cộng bao nhiêu ô nhớ trong toàn bộ quá trình này? Chúng ta sẽ có tổng cộng bao nhiêu ô nhớ tại một thời điểm, giả sử rằng việc thu gom rác xảy ra ngay khi tham chiếu cuối cùng đến một mảng bị mất?

**Bài tập 2.5.6:** Bắt đầu từ một mảng có kích thước 100, khoảng bao nhiêu ô nhớ được tạo và lấp đầy nếu chúng ta gọi `addLast` 1.000 lần?

Việc tạo ra tất cả các ô nhớ đó và sao chép lại nội dung của chúng đều tốn thời gian. Trong biểu đồ dưới đây, ta vẽ tổng thời gian theo số lượng thao tác, với `SLList` ở phía trên và danh sách dựa trên mảng kiểu thô sơ ở phía dưới. Đồ thị của `SLList` là một đường thẳng, nghĩa là mỗi thao tác `add` làm tăng thời gian thêm cùng một lượng. Điều đó có nghĩa là mỗi thao tác riêng lẻ đều mất thời gian hằng số. Bạn cũng có thể hiểu như sau: đồ thị là tuyến tính, nên mỗi phép toán là hằng số, vì tích phân của một hằng số là một đường thẳng.

Ngược lại, đồ thị của danh sách mảng thô sơ có dạng parabol, cho thấy mỗi phép toán mất thời gian tuyến tính, vì tích phân của một đường thẳng là một parabol. Điều này có ý nghĩa rất lớn trong thực tế. Với 100.000 lần chèn, ta có thể ước tính mức chậm đi bằng tỉ lệ `N^2 / N`. Chèn 100.000 phần tử vào danh sách dựa trên mảng theo cách này sẽ mất thời gian lâu hơn khoảng 100.000 lần. Rõ ràng điều đó là không thể chấp nhận được.

![fig25/insert\_experiment.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig25/insert_experiment.png)

#### Thay đổi kích thước theo hệ số nhân <a href="#geometric-resizing" id="geometric-resizing"></a>

Ta có thể khắc phục các vấn đề hiệu năng bằng cách tăng kích thước mảng theo hệ số nhân, thay vì theo lượng cộng thêm. Nghĩa là, thay vì **thêm** vào một số ô nhớ bằng một hệ số thay đổi kích thước `RFACTOR`:

```java
public void insertBack(int x) {
    if (size == items.length) {
           resize(size + RFACTOR);
    }
    items[size] = x;
    size += 1;
}
```

Thay vào đó, ta thay đổi kích thước bằng cách **nhân** số lượng ô nhớ với `RFACTOR`.

```java
public void insertBack(int x) {
    if (size == items.length) {
           resize(size * RFACTOR);
    }
    items[size] = x;
    size += 1;
}
```

Nếu lặp lại thí nghiệm tính toán trước đó, ta sẽ thấy `AList` mới hoàn thành 100.000 lần chèn nhanh đến mức gần như không nhận ra. Phân tích đầy đủ lý do tại sao điều này xảy ra sẽ được để tới chương cuối của cuốn sách.

#### Hiệu năng bộ nhớ <a href="#memory-performance" id="memory-performance"></a>

`AList` của ta gần như đã hoàn thiện, nhưng vẫn còn một vấn đề lớn. Giả sử ta chèn 1.000.000.000 phần tử, rồi sau đó xóa 990.000.000 phần tử. Khi đó, ta chỉ sử dụng 10.000.000 ô nhớ, còn 99% hoàn toàn bị bỏ không.

Để khắc phục vấn đề này, ta cũng có thể thu nhỏ mảng khi nó bắt đầu quá trống. Cụ thể, ta định nghĩa "tỉ lệ sử dụng" `R` bằng kích thước của danh sách chia cho độ dài của mảng `items`. Ví dụ, trong hình dưới đây, tỉ lệ sử dụng là `0.04`.

![fig25/usage\_ratio.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig25/usage_ratio.png)

Trong một cách cài đặt điển hình, ta sẽ giảm một nửa kích thước mảng khi `R` giảm xuống dưới `0.25`.

#### AList generic <a href="#generic-alists" id="generic-alists"></a>

Giống như trước đây, ta có thể sửa đổi `AList` để nó chứa được bất kỳ kiểu dữ liệu nào, chứ không chỉ số nguyên. Để làm điều này, ta lại dùng ký hiệu ngoặc nhọn đặc biệt trong phần khai báo lớp, rồi thay kiểu `int` ở những chỗ phù hợp bằng một tham số kiểu tùy ý. Ví dụ dưới đây dùng `Glorp` làm tham số kiểu.

Có một điểm khác biệt quan trọng về cú pháp: Java không cho phép ta tạo một mảng các đối tượng generic do một chi tiết rắc rối trong cách generics được triển khai. Nghĩa là, ta không thể viết như sau:

```java
Glorp[] items = new Glorp[8];
```

Thay vào đó, ta phải dùng cú pháp hơi khó chịu dưới đây:

```java
Glorp[] items = (Glorp []) new Object[8];
```

Điều này sẽ sinh ra một cảnh báo biên dịch, nhưng đó chỉ là điều ta phải chấp nhận. Ta sẽ bàn kỹ hơn về điểm này trong một chương sau.

Thay đổi còn lại là ta sẽ gán `null` cho bất kỳ phần tử nào mà ta "xóa". Trước đây, ta không có lý do đặc biệt để làm rỗng các phần tử đã bị xóa; nhưng với các đối tượng generic, ta thực sự muốn xóa bỏ các tham chiếu tới những đối tượng đang được lưu. Mục đích là để tránh hiện tượng *loitering* - giữ lại các tham chiếu thừa. Hãy nhớ rằng Java chỉ hủy đối tượng khi tham chiếu cuối cùng tới nó đã mất. Nếu ta không xóa tham chiếu đi, Java sẽ không thể thu gom rác các đối tượng đã từng được thêm vào danh sách.

Đây là một lỗi hiệu năng tinh vi mà bạn khó nhận ra nếu không chủ động tìm nó, nhưng trong một số trường hợp nó có thể gây lãng phí bộ nhớ đáng kể.
