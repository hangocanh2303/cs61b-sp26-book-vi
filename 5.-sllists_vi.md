# 5. SLLists

Trong Chương 3, chúng ta đã xây dựng lớp `IntList`, một cấu trúc dữ liệu danh sách về mặt kỹ thuật có thể làm mọi điều mà một danh sách cần làm. Tuy nhiên, trên thực tế, `IntList` khá vụng về khi sử dụng, dẫn đến mã khó đọc và khó bảo trì.

Về cơ bản, vấn đề là `IntList` là cái mà tôi gọi là cấu trúc dữ liệu **đệ quy lộ thiên**. Để sử dụng `IntList` một cách đúng đắn, lập trình viên phải hiểu và vận dụng đệ quy ngay cả với những thao tác danh sách đơn giản. Điều này làm giảm tính hữu dụng của nó đối với người mới học, đồng thời có thể kéo theo cả một lớp lỗi khó mới tùy thuộc vào những phương thức hỗ trợ mà lớp `IntList` cung cấp.

Lấy cảm hứng từ trải nghiệm với `IntList`, giờ đây ta sẽ xây dựng một lớp mới tên `SLList`, gần với cách các danh sách thường được triển khai trong các ngôn ngữ hiện đại hơn nhiều. Ta sẽ làm điều này bằng cách lần lượt thêm vào một chuỗi cải tiến.

#### Cải tiến #1: Đổi thương hiệu <a href="#improvement-1-rebranding" id="improvement-1-rebranding"></a>

Lớp `IntList` của chúng ta lần trước như sau, với các phương thức trợ giúp bị bỏ qua:

```java
public class IntList {
    public int first;
    public IntList rest;

    public IntList(int f, IntList r) {
        first = f;
        rest = r;
    }
...
```

Bước đầu tiên của chúng ta sẽ chỉ đơn giản là đổi tên mọi thứ và loại bỏ các phương thức trợ giúp. Điều này có vẻ không có vẻ gì là tiến bộ, nhưng hãy tin tôi đi, tôi là người chuyên nghiệp.

```java
public class IntNode {
    public int item;
    public IntNode next;

    public IntNode(int i, IntNode n) {
        item = i;
        next = n;
    }
}
```

#### Cải tiến #2: Quan liêu <a href="#improvement-2-bureaucracy" id="improvement-2-bureaucracy"></a>

Biết rằng `IntNode` khá khó làm việc trực tiếp, ta sẽ tạo ra một lớp riêng tên là `SLList` để người dùng tương tác với nó. Dạng cơ bản của lớp này chỉ đơn giản là:

```java
public class SLList {
    public IntNode first;

    public SLList(int x) {
        first = new IntNode(x, null);
    }
}
```

Hiện tại, chúng ta đã có thể hiểu mơ hồ về lý do tại sao `SLList` lại tốt hơn. So sánh việc tạo `IntList` của một mục với việc tạo `SLList` của một mục.

```java
IntList L1 = new IntList(5, null);
SLList L2  = new SLList(5);
```

`SLList` che giấu khỏi người dùng chi tiết rằng tồn tại một liên kết `null`. Bản thân lớp `SLList` lúc này vẫn chưa thực sự hữu ích, nên hãy thêm các phương thức `addFirst` và `getFirst` như những bài khởi động đơn giản. Hãy thử tự viết chúng trước khi đọc tiếp.

#### addFirst và getFirst <a href="#addfirst-and-getfirst" id="addfirst-and-getfirst"></a>

`addFirst` khá đơn giản nếu bạn đã hiểu chương 2.1. Với `IntList`, ta thêm vào đầu danh sách bằng dòng mã `L = new IntList(5, L)`. Vì vậy, ta thu được:

```java
public class SLList {
    public IntNode first;

    public SLList(int x) {
        first = new IntNode(x, null);
    }

    /** Adds an item to the front of the list. */
    public void addFirst(int x) {
        first = new IntNode(x, first);
    }
}
```

`getFirst` thậm chí còn dễ dàng hơn. Chúng ta chỉ cần trả về `first.item`:

```java
/** Retrieves the front item from the list. */
public int getFirst() {
    return first.item;
}
```

Kết quả là lớp `SLList` dễ sử dụng hơn nhiều. So sánh:

```java
SLList L = new SLList(15);
L.addFirst(10);
L.addFirst(5);
int x = L.getFirst();
```

tương đương với `IntList`:

```java
IntList L = new IntList(15, null);
L = new IntList(10, L);
L = new IntList(5, L);
int x = L.first;
```

So sánh trực quan hai cấu trúc dữ liệu, chúng ta có: (với phiên bản `IntList` ở trên và phiên bản `SLList` ở dưới)

![IntList\_vs\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/IntList_vs_SLList.png)

Về cơ bản, lớp `SLList` đóng vai trò trung gian giữa người dùng danh sách và cấu trúc dữ liệu đệ quy lộ thiên bên dưới. Như gợi ý ở ví dụ `IntList` phía trên, có một khả năng không mong muốn là người dùng `IntList` có thể giữ các biến trỏ vào giữa danh sách. Như Ovid đã nói: [Con người không thể nhìn vào một vị thần mà không chết](https://en.wikipedia.org/wiki/Semele), nên có lẽ tốt hơn là để `SLList` đứng ra làm trung gian.

**Bài tập 2.2.1**: Một độc giả tò mò có thể phản đối rằng `IntList` cũng sẽ dễ dùng như vậy nếu ta chỉ viết thêm một phương thức `addFirst`. Hãy thử tự viết phương thức `addFirst` cho lớp `IntList`. Bạn sẽ thấy phương thức thu được vừa rắc rối vừa kém hiệu quả.

#### Cải tiến #3: Public và Private <a href="#improvement-3-public-vs-private" id="improvement-3-public-vs-private"></a>

Không may là `SLList` của chúng ta vẫn có thể bị bỏ qua, và người dùng vẫn có thể truy cập trực tiếp vào cấu trúc dữ liệu lộ thiên bên dưới cùng mọi rủi ro của nó. Một lập trình viên có thể dễ dàng sửa đổi danh sách trực tiếp mà không đi qua phương thức `addFirst` "an toàn, đã được kiểm chứng", ví dụ:

```java
SLList L = new SLList(15);
L.addFirst(10);
L.first.next.next = L.first.next;
```

![bad\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/bad_SLList.png)

Điều này dẫn đến một danh sách không đúng định dạng với vòng lặp vô hạn. Để giải quyết vấn đề này, chúng ta có thể sửa đổi lớp `SLList` để biến `first` được khai báo bằng từ khóa `private`.

```java
public class SLList {
    private IntNode first;
...
```

Các biến và phương thức `private` chỉ có thể được truy cập bởi mã bên trong cùng tệp `.java`, ở đây là `SLList.java`. Điều đó có nghĩa là một lớp như `SLLTroubleMaker` dưới đây sẽ không biên dịch được, và sẽ cho lỗi `first has private access in SLList`.

```java
public class SLLTroubleMaker {
    public static void main(String[] args) {
        SLList L = new SLList(15);
        L.addFirst(10);
        L.first.next.next = L.first.next;
    }
}
```

Ngược lại, bất kỳ mã nào bên trong tệp `SLList.java` sẽ có thể truy cập vào biến `first`.

Việc hạn chế quyền truy cập có vẻ hơi ngớ ngẩn. Xét cho cùng, điều duy nhất từ khóa `private` làm là khiến một số chương trình vốn dĩ biên dịch được nay không còn biên dịch được nữa. Tuy nhiên, trong các dự án phần mềm lớn, `private` là một tín hiệu vô giá cho biết có những đoạn mã mà người dùng cuối nên bỏ qua, và do đó không cần phải hiểu. Tương tự, từ khóa `public` nên được hiểu như một lời cam kết rằng một phương thức sẵn sàng cho người dùng và sẽ hoạt động **mãi mãi** đúng như hiện tại.

Tương tự, một chiếc ô tô có một số bộ phận “công khai” theo nghĩa `public`, ví dụ bàn đạp ga và phanh. Dưới mui xe là các chi tiết “riêng” theo nghĩa `private` về cách chúng hoạt động. Ở ô tô chạy bằng xăng, bàn đạp ga có thể điều khiển một số loại hệ thống phun nhiên liệu và ở ô tô chạy bằng pin, nó có thể điều chỉnh lượng năng lượng pin được cung cấp cho động cơ. Mặc dù các chi tiết “riêng” có thể khác nhau giữa các xe, ta vẫn mong đợi hành vi tương tự ở mọi bàn đạp ga. Việc thay đổi những điều này sẽ gây ra sự kinh ngạc lớn cho người dùng và có thể gây ra những tai nạn khủng khiếp.

**Khi bạn tạo một thành viên `public` (tức là phương thức hoặc biến), hãy cẩn thận, vì bạn đang cam kết hỗ trợ hành vi của thành viên đó một cách chính xác như hiện tại, mãi mãi.**

#### Cải tiến #4: Lớp lồng nhau <a href="#improvement-4-nested-classes" id="improvement-4-nested-classes"></a>

Hiện tại, ta có hai tệp `.java`: `IntNode` và `SLList`. Tuy nhiên, `IntNode` thực chất chỉ là phần phụ trong câu chuyện của `SLList`.

Java cung cấp cho chúng ta khả năng nhúng một khai báo lớp bên trong một khai báo lớp khác chỉ cho tình huống này. Cú pháp rất đơn giản và trực quan:

```java
public class SLList {
       public class IntNode {
            public int item;
            public IntNode next;
            public IntNode(int i, IntNode n) {
                item = i;
                next = n;
            }
       }

       private IntNode first; 

       public SLList(int x) {
           first = new IntNode(x, null);
       } 
...
```

Việc có một lớp lồng nhau không có tác dụng đáng kể nào đối với hiệu suất mã và chỉ đơn giản là một công cụ để giữ cho mã có tổ chức. Để biết thêm về các lớp lồng nhau, hãy xem [Tài liệu chính thức của Oracle](https://docs.oracle.com/javase/tutorial/java/javaOO/nested.html).

Nếu lớp lồng nhau không có nhu cầu sử dụng bất kỳ phương thức thực thể hoặc biến thực thể nào của `SLList`, bạn có thể khai báo lớp lồng nhau `static`, như sau. Khai báo một lớp lồng nhau là `static` có nghĩa là các phương thức bên trong lớp lồng nhau tĩnh không thể truy cập bất kỳ thành viên nào của lớp bao ngoài. Trong trường hợp này, điều đó có nghĩa là không có phương thức nào trong `IntNode` có thể truy cập `first`, `addFirst` hoặc `getFirst`.

```java
public class SLList {
       public static class IntNode {
            public int item;
            public IntNode next;
            public IntNode(int i, IntNode n) {
                item = i;
                next = n;
            }
       }

       private IntNode first;
...
```

Điều này giúp tiết kiệm một chút bộ nhớ, vì mỗi `IntNode` không còn cần phải theo dõi cách truy cập `SLList` kèm theo của nó nữa.

Nói cách khác, nếu bạn kiểm tra đoạn mã trên, bạn sẽ thấy rằng lớp `IntNode` không bao giờ sử dụng biến `first` của `SLList`, cũng như bất kỳ phương thức nào của `SLList`. Kết quả là, chúng ta có thể sử dụng từ khóa `static`, có nghĩa là lớp `IntNode` không giữ tham chiếu ngầm tới lớp bao `SLList`, giúp tiết kiệm một lượng nhỏ bộ nhớ.

Nếu điều này có vẻ hơi kỹ thuật và khó thực hiện, hãy thử Bài tập 2.2.2. Một nguyên tắc đơn giản là _nếu bạn không sử dụng bất kỳ thành viên thực thể nào của lớp bên ngoài, hãy đặt lớp lồng nhau thành `static`_.

**Bài tập 2.2.2** Xóa từ `static` càng ít càng tốt để [chương trình này](https://joshhug.gitbooks.io/hug61b/content/chap2/exercises/Government.java) biên dịch (Làm mới trang sau khi nhấp vào liên kết và đảm bảo url đã thay đổi). Hãy nhớ đọc các nhận xét ở trên cùng trước khi thực hiện bài tập.

#### addLast() và size() <a href="#addlast-and-size" id="addlast-and-size"></a>

Để đi tiếp các cải tiến còn lại và minh họa một số mẫu phổ biến khi triển khai cấu trúc dữ liệu, ta sẽ thêm các phương thức `addLast(int x)` và `size()`. Bạn nên dùng [mã khởi đầu](https://github.com/Berkeley-CS61B/lectureCode/blob/master/lists2/DIY/addLastAndSize/SLList.java) và tự mình thử trước khi đọc tiếp. Tôi đặc biệt khuyến khích bạn thử viết một triển khai đệ quy của `size`, điều này sẽ mang lại một thử thách thú vị.

Tôi sẽ triển khai lặp lại phương thức `addLast`, mặc dù bạn cũng có thể thực hiện theo cách đệ quy. Ý tưởng này khá đơn giản, chúng ta tạo một biến con trỏ `p` và lặp nó từ đầu đến cuối danh sách.

```java
/** Adds an item to the end of the list. */
public void addLast(int x) {
    IntNode p = first;

    /* Advance p to the end of the list. */
    while (p.next != null) {
        p = p.next;
    }
    p.next = new IntNode(x, null);
}
```

Ngược lại, tôi sẽ triển khai `size` theo cách đệ quy. Phương thức này sẽ hơi giống với phương thức `size` mà chúng ta đã triển khai trong phần [2.1](https://joshhug.gitbooks.io/hug61b/content/chap2/chap21.html) cho `IntList`.

Lời gọi đệ quy cho `size` trong `IntList` rất đơn giản: `return 1 + this.rest.size()`. Nhưng với `SLList`, cách tiếp cận này không còn hợp lý nữa. `SLList` không có biến `rest`. Thay vào đó, ta sẽ dùng một mẫu phổ biến với các lớp trung gian như `SLList`: tạo một phương thức trợ giúp `private` tương tác trực tiếp với cấu trúc dữ liệu đệ quy lộ thiên bên dưới.

Ta thu được một phương thức trợ giúp như sau:

```java
/** Returns the size of the list starting at IntNode p. */
private static int size(IntNode p) {
    if (p.next == null) {
        return 1;
    }

    return 1 + size(p.next);
}
```

Dùng phương thức trợ giúp này, ta có thể dễ dàng tính kích thước của toàn bộ danh sách:

```java
public int size() {
    return size(first);
}
```

Ở đây, chúng ta có hai phương thức, cả hai đều được đặt tên là `size`. Điều này được cho phép trong Java vì chúng có các tham số khác nhau. Chúng ta nói rằng hai phương thức có cùng tên nhưng có chữ ký khác nhau sẽ bị **quá tải**. Để biết thêm về các phương thức nạp chồng, hãy xem [tài liệu chính thức](https://docs.oracle.com/javase/tutorial/java/javaOO/methods.html) của Java.

Một cách tiếp cận khác là tạo một phương thức trợ giúp không tĩnh trong chính lớp `IntNode`. Cả hai cách tiếp cận đều ổn, mặc dù cá nhân tôi không thích có bất kỳ phương thức nào trong lớp `IntNode`.

## Cải tiến #5: Bộ nhớ đệm <a href="#improvement-5-caching" id="improvement-5-caching"></a>

Hãy xem xét phương thức `size` mà ta đã viết ở trên. Giả sử `size` mất 2 giây trên danh sách có kích thước 1.000. Ta kỳ vọng rằng trên danh sách có kích thước 1.000.000, phương thức `size` sẽ mất 2.000 giây vì máy tính phải duyệt qua số mục trong danh sách gấp 1.000 lần để đến cuối. Việc có một phương thức `size` rất chậm đối với các danh sách lớn là không thể chấp nhận được vì chúng ta có thể làm tốt hơn.

Có thể viết lại `size` sao cho nó tốn cùng một khoảng thời gian, bất kể danh sách có lớn đến đâu.

Để làm như vậy, chúng ta chỉ cần thêm một biến `size` vào lớp `SLList` để theo dõi kích thước hiện tại, tạo ra mã bên dưới. Cách lưu dữ liệu quan trọng này để tăng tốc độ truy xuất đôi khi được gọi là **lưu vào bộ nhớ đệm**.

```java
public class SLList {
    ... /* IntNode declaration omitted. */
    private IntNode first;
    private int size;

    public SLList(int x) {
        first = new IntNode(x, null);
        size = 1;
    }

    public void addFirst(int x) {
        first = new IntNode(x, first);
        size += 1;
    }

    public int size() {
        return size;
    }
    ...
}
```

Sửa đổi này làm cho phương thức `size` cực kỳ nhanh, bất kể danh sách lớn đến đâu. Tất nhiên, nó cũng làm chậm nhẹ `addFirst` và `addLast`, và tăng một chút bộ nhớ dùng cho lớp, nhưng mức tăng không đáng kể. Trong trường hợp này, sự cân bằng rõ ràng có lợi cho việc tạo bộ đệm cho kích thước.

#### Cải tiến #6: Danh sách trống <a href="#improvement-6-the-empty-list" id="improvement-6-the-empty-list"></a>

`SLList` của ta có một số lợi ích so với `IntList` đơn giản ở chương 2.1:

* Người dùng `SLList` không bao giờ nhìn thấy lớp `IntList`.
  * Đơn giản hơn để sử dụng.
  * Phương thức `addFirst` hiệu quả hơn (bài tập 2.2.1).
  * Tránh lỗi hoặc hành vi sai trái của người dùng `IntList`.
* Phương thức `size` nhanh hơn có thể với `IntList`.

Một lợi thế tự nhiên khác là chúng ta có thể dễ dàng triển khai hàm tạo để tạo danh sách trống. Cách tự nhiên nhất là đặt `first` thành `null` nếu danh sách trống. Điều này mang lại hàm tạo dưới đây:

```java
public SLList() {
    first = null;
    size = 0;
}
```

Thật không may, điều này khiến phương thức `addLast` của chúng ta bị lỗi nếu chúng ta chèn vào một danh sách trống. Vì `first` là `null`, nên việc cố gắng truy cập `p.next` trong `while (p.next != null)` bên dưới sẽ gây ra ngoại lệ con trỏ null.

```java
public void addLast(int x) {
    size += 1;
    IntNode p = first;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

**Bài tập 2.2.3** Sửa phương thức `addLast`. Mã khởi đầu [đây](https://github.com/Berkeley-CS61B/lectureCode/blob/master/lists2/DIY/fixAddLast/SLList.java).

#### Cải tiến #6b: Nút sentinel <a href="#improvement-6b-sentinel-nodes" id="improvement-6b-sentinel-nodes"></a>

Một giải pháp để khắc phục `addLast` là tạo một trường hợp đặc biệt cho danh sách trống, như hiển thị bên dưới:

```java
public void addLast(int x) {
    size += 1;

    if (first == null) {
        first = new IntNode(x, null);
        return;
    }

    IntNode p = first;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

Giải pháp này hoạt động nhưng nên tránh mã trường hợp đặc biệt như được hiển thị ở trên khi cần thiết. Con người chỉ có rất nhiều trí nhớ làm việc, và do đó chúng ta muốn kiểm soát sự phức tạp bất cứ khi nào có thể. Đối với cấu trúc dữ liệu đơn giản như `SLList`, số lượng trường hợp đặc biệt là ít. Các cấu trúc dữ liệu phức tạp hơn như cây cối có thể trở nên xấu hơn rất nhiều.

Một giải pháp rõ ràng hơn (dù ban đầu có vẻ trừu tượng hơn) là làm cho mọi `SLList` đều "cùng một dạng", kể cả khi rỗng. Ta có thể làm điều này bằng cách luôn có một nút đặc biệt tồn tại, gọi là **nút sentinel** (hay gọi tắt là **sentinel**). Sentinel sẽ chứa một giá trị mà ta không quan tâm.

Ví dụ: danh sách trống được tạo bởi `SLList L = new SLList()` sẽ như dưới đây:

![empty\_sentinelized\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/empty_sentinelized_SLList.png)

Và một `SLList` với các mục 5, 10 và 15 sẽ trông như sau:

![three\_item\_sentenlized\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/three_item_sentenlized_SLList.png)

Trong các hình trên, ô màu tím nhạt với nhãn `??` ở `item` cho biết ta không quan tâm giá trị tại đó là gì. Vì Java không cho phép điền số nguyên bằng dấu `?`, trong mã ta chỉ chọn một hằng tùy ý (ví dụ `-518273`, `63`, v.v.).

Khi đã có sentinel, ta không cần nhánh đặc biệt cho danh sách rỗng nữa, nên chỉ cần bỏ nhánh đó khỏi `addLast`, thu được:

```java
public void addLast(int x) {
    size += 1;
    IntNode p = sentinel;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

Như bạn có thể thấy, mã này sạch hơn nhiều!

#### Bất biến <a href="#invariants" id="invariants"></a>

Bất biến là một thực tế về cấu trúc dữ liệu được đảm bảo là đúng (giả sử không có lỗi trong mã của bạn).

Một `SLList` dùng sentinel có ít nhất các bất biến sau:

* Tham chiếu `sentinel` luôn trỏ tới nút sentinel.
* Mục phía trước (nếu nó tồn tại), luôn ở `sentinel.next.item`.
* Biến `size` luôn là tổng số mục đã được thêm vào.

Tính bất biến giúp bạn dễ dàng suy luận về mã hơn và cũng cung cấp cho bạn các mục tiêu cụ thể để phấn đấu nhằm đảm bảo mã của bạn hoạt động.

Để thấy hết tiện lợi của sentinel, bạn cần tự triển khai vài phiên bản. Bạn sẽ thực hành nhiều trong Dự án 1. Tuy nhiên, ta khuyên bạn đợi đến khi đọc xong phần tiếp theo của sách trước khi bắt đầu Dự án 1.
