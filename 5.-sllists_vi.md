# 5. SLLists

Trong Chương 3, chúng ta đã xây dựng lớp `IntList`, một cấu trúc dữ liệu danh sách về mặt kỹ thuật có thể làm mọi điều mà một danh sách cần làm. Tuy nhiên, trên thực tế, `IntList` khá vụng về khi sử dụng, dẫn đến mã khó đọc và khó bảo trì.

Về cơ bản, vấn đề là `IntList` là cái mà tôi gọi là cấu trúc dữ liệu **đệ quy lộ thiên**. Để sử dụng `IntList` một cách đúng đắn, lập trình viên phải hiểu và vận dụng đệ quy ngay cả với những thao tác danh sách đơn giản. Điều này làm giảm tính hữu dụng của nó đối với người mới học, đồng thời có thể kéo theo cả một lớp lỗi khó mới tùy thuộc vào những phương thức hỗ trợ mà lớp `IntList` cung cấp.

Lấy cảm hứng từ trải nghiệm với `IntList`, giờ đây ta sẽ xây dựng một lớp mới tên `SLList`, gần với cách các danh sách thường được triển khai trong các ngôn ngữ hiện đại hơn nhiều. Ta sẽ làm điều này bằng cách lần lượt thêm vào một chuỗi những cải tiến.

#### Cải tiến #1: Tái định danh - Rebranding <a href="#improvement-1-rebranding" id="improvement-1-rebranding"></a>

Lớp `IntList` của chúng ta lần trước như sau, với các phương thức trợ giúp bị bỏ qua:

```java
public class IntList {
    public int first;
    public IntList rest;

    public IntList(int f, IntList r) {
        first = f;
        rest = r;
    }
...
```

Bước đầu tiên của chúng ta sẽ chỉ đơn giản là đổi tên mọi thứ và bỏ đi các phương thức trợ giúp. Việc này có vẻ không phải là một bước tiến bộ, nhưng hãy tin tôi đi, tôi là dân chuyên nghiệp mà.

```java
public class IntNode {
    public int item;
    public IntNode next;

    public IntNode(int i, IntNode n) {
        item = i;
        next = n;
    }
}
```

#### Cải tiến #2: Cơ chế trung gian <a href="#improvement-2-bureaucracy" id="improvement-2-bureaucracy"></a>

Biết rằng làm việc trực tiếp với `IntNode` khá vụng về, ta sẽ tạo một lớp riêng tên là `SLList` để người dùng tương tác với. Dạng cơ bản của lớp này chỉ đơn giản như sau:

```java
public class SLList {
    public IntNode first;

    public SLList(int x) {
        first = new IntNode(x, null);
    }
}
```

Đã có thể thấy sơ bộ vì sao `SLList` tốt hơn. Hãy so sánh việc tạo một `IntList` chứa một phần tử với việc tạo một `SLList` chứa một phần tử.

```java
IntList L1 = new IntList(5, null);
SLList L2  = new SLList(5);
```

`SLList` giấu đi khỏi người dùng chi tiết rằng có một liên kết `null`. Lớp `SLList` lúc này vẫn chưa thật sự hữu ích, nên ta sẽ thêm hai phương thức `addFirst` và `getFirst` để khởi động. Hãy thử tự viết chúng trước khi đọc tiếp.

#### addFirst và getFirst <a href="#addfirst-and-getfirst" id="addfirst-and-getfirst"></a>

`addFirst` khá đơn giản nếu bạn đã hiểu chương 2.1. Với `IntList`, ta thêm vào đầu danh sách bằng dòng mã `L = new IntList(5, L)`. Vì vậy, ta thu được:

```java
public class SLList {
    public IntNode first;

    public SLList(int x) {
        first = new IntNode(x, null);
    }

    /** Adds an item to the front of the list. */
    public void addFirst(int x) {
        first = new IntNode(x, first);
    }
}
```

`getFirst` thậm chí còn dễ dàng hơn. Chúng ta chỉ cần trả về `first.item`:

```java
/** Retrieves the front item from the list. */
public int getFirst() {
    return first.item;
}
```

Kết quả là lớp `SLList` dễ sử dụng hơn nhiều. So sánh:

```java
SLList L = new SLList(15);
L.addFirst(10);
L.addFirst(5);
int x = L.getFirst();
```

so với đoạn `IntList` tương đương:

```java
IntList L = new IntList(15, null);
L = new IntList(10, L);
L = new IntList(5, L);
int x = L.first;
```

So sánh trực quan hai cấu trúc dữ liệu, chúng ta có: (với phiên bản `IntList` ở trên và phiên bản `SLList` ở dưới)

![IntList\_vs\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/IntList_vs_SLList.png)

Về cơ bản, lớp `SLList` đóng vai trò trung gian giữa người dùng danh sách và cấu trúc dữ liệu đệ quy lộ thiên bên dưới. Như đã gợi ý ở phiên bản `IntList` phía trên, có một khả năng không mong muốn: người dùng `IntList` có thể có những biến trỏ vào giữa `IntList`. Như Ovid đã nói: [Con người không thể nhìn vào một vị thần mà không chết](https://en.wikipedia.org/wiki/Semele), nên có lẽ tốt hơn là để `SLList` đứng ra làm trung gian cho chúng ta.

**Bài tập 2.2.1**: Một độc giả tò mò có thể phản đối rằng `IntList` cũng sẽ dễ dùng như vậy nếu ta chỉ viết thêm một phương thức `addFirst`. Hãy thử tự viết phương thức `addFirst` cho lớp `IntList`. Bạn sẽ thấy phương thức thu được vừa rắc rối vừa kém hiệu quả.

#### Cải tiến #3: Public và Private <a href="#improvement-3-public-vs-private" id="improvement-3-public-vs-private"></a>

Không may là `SLList` của chúng ta vẫn có thể bị vượt mặt, và người dùng vẫn có thể chạm tới sức mạnh thô của cấu trúc dữ liệu lộ thiên bên dưới (cùng mọi rủi ro đi kèm). Một lập trình viên có thể dễ dàng sửa danh sách trực tiếp mà không đi qua phương thức `addFirst` "đã được kiểm định, được mẹ chấp thuận", ví dụ:

```java
SLList L = new SLList(15);
L.addFirst(10);
L.first.next.next = L.first.next;
```

![bad\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/bad_SLList.png)

Kết quả là một danh sách bị méo mó với một vòng lặp vô hạn. Để xử lý vấn đề này, ta có thể sửa lớp `SLList` sao cho biến `first` được khai báo với từ khóa `private`.

```java
public class SLList {
    private IntNode first;
...
```

Các biến và phương thức `private` chỉ có thể được truy cập bởi mã bên trong cùng tệp `.java`, ở đây là `SLList.java`. Nghĩa là một lớp như `SLLTroubleMaker` dưới đây sẽ không biên dịch được và báo lỗi `first has private access in SLList`.

```java
public class SLLTroubleMaker {
    public static void main(String[] args) {
        SLList L = new SLList(15);
        L.addFirst(10);
        L.first.next.next = L.first.next;
    }
}
```

Ngược lại, mọi mã bên trong tệp `SLList.java` đều có thể truy cập biến `first`.

Việc hạn chế quyền truy cập có vẻ hơi vô nghĩa. Xét cho cùng, điều duy nhất từ khóa `private` làm là khiến các chương trình vốn biên dịch được nay không còn biên dịch được nữa. Tuy nhiên, trong các dự án phần mềm lớn, `private` là một tín hiệu vô giá cho biết có những đoạn mã mà người dùng cuối nên bỏ qua (và do đó không cần phải hiểu). Tương ứng, từ khóa `public` nên được xem như lời tuyên bố rằng một phương thức là sẵn sàng để dùng và sẽ hoạt động **mãi mãi** đúng như hiện tại.

Lấy một phép so sánh: một chiếc ô tô có một số bộ phận `public`, ví dụ bàn đạp ga và bàn đạp phanh. Bên dưới nắp capô có những chi tiết `private` về cách các bộ phận này vận hành. Ở ô tô chạy xăng, bàn đạp ga có thể điều khiển một hệ thống phun nhiên liệu nào đó; ở ô tô chạy pin, nó có thể điều chỉnh lượng điện năng từ pin đưa tới động cơ. Dù các chi tiết `private` này có thể khác nhau giữa các xe, ta vẫn mong đợi cùng một hành vi từ mọi bàn đạp ga. Thay đổi điều đó sẽ khiến người dùng hoảng hốt và rất có thể dẫn tới những tai nạn khủng khiếp.

**Khi bạn tạo một thành viên `public` (tức là phương thức hoặc biến), hãy cẩn thận, vì bạn đang gần như cam kết duy trì hành vi của thành viên đó đúng như hiện tại, mãi mãi.**

#### Cải tiến #4: Lớp lồng nhau <a href="#improvement-4-nested-classes" id="improvement-4-nested-classes"></a>

Hiện tại, ta có hai tệp `.java`: `IntNode` và `SLList`. Tuy nhiên, `IntNode` thực chất chỉ là một vai phụ trong câu chuyện của `SLList`.

Java cho phép ta nhúng khai báo một lớp vào bên trong một lớp khác đúng cho tình huống này. Cú pháp vừa đơn giản vừa trực quan:

```java
public class SLList {
       public class IntNode {
            public int item;
            public IntNode next;
            public IntNode(int i, IntNode n) {
                item = i;
                next = n;
            }
       }

       private IntNode first; 

       public SLList(int x) {
           first = new IntNode(x, null);
       } 
...
```

Việc có một lớp lồng nhau không ảnh hưởng đáng kể đến hiệu năng mã; nó đơn thuần là một công cụ giúp tổ chức mã. Để biết thêm về lớp lồng nhau, hãy xem [tài liệu chính thức của Oracle](https://docs.oracle.com/javase/tutorial/java/javaOO/nested.html).

Nếu lớp lồng nhau không cần dùng đến bất kỳ phương thức thực thể hay biến thực thể nào của `SLList`, bạn có thể khai báo lớp lồng nhau là `static`, như sau. Khai báo một lớp lồng nhau là `static` có nghĩa là các phương thức bên trong lớp `static` đó không thể truy cập bất kỳ thành viên nào của lớp bao ngoài. Trong trường hợp này, điều đó có nghĩa là không có phương thức nào trong `IntNode` có thể truy cập `first`, `addFirst` hay `getFirst`.

```java
public class SLList {
       public static class IntNode {
            public int item;
            public IntNode next;
            public IntNode(int i, IntNode n) {
                item = i;
                next = n;
            }
       }

       private IntNode first;
...
```

Cách này giúp tiết kiệm một chút bộ nhớ, vì mỗi `IntNode` không còn phải ghi nhớ cách truy cập tới `SLList` bao ngoài nó.

Nói cách khác, nếu kiểm tra đoạn mã ở trên, bạn sẽ thấy lớp `IntNode` không bao giờ dùng đến biến `first` của `SLList`, cũng không dùng bất kỳ phương thức nào của `SLList`. Do đó, ta có thể dùng từ khóa `static`, tức là lớp `IntNode` không giữ tham chiếu tới lớp bao ngoài, giúp tiết kiệm một lượng bộ nhớ nhỏ.

Nếu phần này có vẻ hơi kỹ thuật và khó theo, hãy thử Bài tập 2.2.2. Một nguyên tắc thực hành đơn giản là: _nếu bạn không dùng đến bất kỳ thành viên thực thể nào của lớp bên ngoài, hãy đặt lớp lồng nhau là `static`_.

**Bài tập 2.2.2** Xóa từ `static` càng ít lần càng tốt sao cho [chương trình này](https://joshhug.gitbooks.io/hug61b/content/chap2/exercises/Government.java) biên dịch được (làm mới trang sau khi bấm vào liên kết và kiểm tra rằng URL đã đổi). Nhớ đọc phần chú thích ở đầu file trước khi làm bài.

#### addLast() và size() <a href="#addlast-and-size" id="addlast-and-size"></a>

Để dẫn dắt các cải tiến còn lại và minh họa một số mẫu phổ biến khi cài đặt cấu trúc dữ liệu, ta sẽ thêm phương thức `addLast(int x)` và `size()`. Bạn nên lấy [mã khởi đầu](https://github.com/Berkeley-CS61B/lectureCode/blob/master/lists2/DIY/addLastAndSize/SLList.java) và tự thử trước khi đọc tiếp. Tôi đặc biệt khuyến khích bạn thử viết `size` theo cách đệ quy, đó sẽ là một thử thách thú vị.

Tôi sẽ cài đặt `addLast` theo cách lặp, dù bạn cũng có thể làm theo cách đệ quy. Ý tưởng khá đơn giản: ta tạo một biến con trỏ `p` rồi cho nó đi lần lượt qua danh sách cho đến cuối.

```java
/** Adds an item to the end of the list. */
public void addLast(int x) {
    IntNode p = first;

    /* Advance p to the end of the list. */
    while (p.next != null) {
        p = p.next;
    }
    p.next = new IntNode(x, null);
}
```

Ngược lại, tôi sẽ cài đặt `size` theo cách đệ quy. Phương thức này sẽ hơi giống với phương thức `size` mà ta đã cài đặt ở phần [2.1](https://joshhug.gitbooks.io/hug61b/content/chap2/chap21.html) cho `IntList`.

Lời gọi đệ quy cho `size` trong `IntList` rất gọn: `return 1 + this.rest.size()`. Với `SLList`, cách tiếp cận này không còn hợp lý vì `SLList` không có biến `rest`. Thay vào đó, ta sẽ dùng một mẫu phổ biến với các lớp trung gian như `SLList`: tạo một phương thức trợ giúp `private` tương tác với cấu trúc dữ liệu đệ quy lộ thiên bên dưới.

Ta sẽ thu được một phương thức như sau:

```java
/** Returns the size of the list starting at IntNode p. */
private static int size(IntNode p) {
    if (p.next == null) {
        return 1;
    }

    return 1 + size(p.next);
}
```

Dùng phương thức này, ta có thể dễ dàng tính kích thước của toàn bộ danh sách:

```java
public int size() {
    return size(first);
}
```

Ở đây, ta có hai phương thức cùng tên là `size`. Điều này được cho phép trong Java vì chúng có tham số khác nhau. Ta nói rằng hai phương thức có cùng tên nhưng khác chữ ký thì **nạp chồng** (overloaded). Để biết thêm về các phương thức nạp chồng, hãy xem [tài liệu chính thức](https://docs.oracle.com/javase/tutorial/java/javaOO/methods.html) của Java.

Một cách tiếp cận thay thế là tạo một phương thức trợ giúp không `static` ngay trong chính lớp `IntNode`. Cả hai cách đều ổn, dù cá nhân tôi không thích đặt bất kỳ phương thức nào trong lớp `IntNode`.

## Cải tiến #5: Bộ nhớ đệm <a href="#improvement-5-caching" id="improvement-5-caching"></a>

Hãy xét phương thức `size` mà ta đã viết ở trên. Giả sử `size` mất 2 giây trên một danh sách có kích thước 1.000. Ta kỳ vọng rằng với một danh sách có kích thước 1.000.000, phương thức `size` sẽ mất 2.000 giây, vì máy tính phải đi qua gấp 1.000 lần số phần tử trong danh sách để tới cuối. Việc `size` rất chậm trên các danh sách lớn là không thể chấp nhận được, vì ta có thể làm tốt hơn.

Có thể viết lại `size` để nó tốn cùng một khoảng thời gian, bất kể danh sách lớn đến đâu.

Để làm điều đó, ta chỉ cần thêm một biến `size` vào lớp `SLList` để theo dõi kích thước hiện tại, dẫn tới đoạn mã dưới đây. Cách lưu trữ những dữ liệu quan trọng nhằm tăng tốc truy xuất đôi khi được gọi là **caching**.

```java
public class SLList {
    ... /* IntNode declaration omitted. */
    private IntNode first;
    private int size;

    public SLList(int x) {
        first = new IntNode(x, null);
        size = 1;
    }

    public void addFirst(int x) {
        first = new IntNode(x, first);
        size += 1;
    }

    public int size() {
        return size;
    }
    ...
}
```

Thay đổi này làm cho phương thức `size` cực kỳ nhanh, bất kể danh sách lớn đến đâu. Tất nhiên, nó cũng làm `addFirst` và `addLast` chậm đi, và làm tăng mức sử dụng bộ nhớ của lớp, nhưng chỉ với một lượng không đáng kể. Trong trường hợp này, đánh đổi rõ ràng nghiêng về việc tạo một cache cho kích thước.

#### Cải tiến #6: Danh sách trống <a href="#improvement-6-the-empty-list" id="improvement-6-the-empty-list"></a>

`SLList` của ta có nhiều ưu điểm so với `IntList` đơn giản ở chương 2.1:

* Người dùng `SLList` không bao giờ nhìn thấy lớp `IntList`.
  * Đơn giản hơn khi dùng.
  * Phương thức `addFirst` hiệu quả hơn (bài tập 2.2.1).
  * Tránh được những lỗi hoặc hành vi xấu từ phía người dùng `IntList`.
* Phương thức `size` nhanh hơn mức mà `IntList` có thể đạt được.

Một ưu điểm tự nhiên khác là ta sẽ dễ dàng cài đặt một constructor để tạo danh sách rỗng. Cách tự nhiên nhất là đặt `first` thành `null` khi danh sách rỗng. Điều này dẫn tới constructor dưới đây:

```java
public SLList() {
    first = null;
    size = 0;
}
```

Đáng tiếc, điều này khiến phương thức `addLast` của ta bị lỗi nếu ta chèn vào một danh sách rỗng. Vì `first` là `null`, việc cố gắng truy cập `p.next` trong `while (p.next != null)` bên dưới sẽ gây ra `NullPointerException`.

```java
public void addLast(int x) {
    size += 1;
    IntNode p = first;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

**Bài tập 2.2.3** Hãy sửa phương thức `addLast`. Mã khởi đầu [ở đây](https://github.com/Berkeley-CS61B/lectureCode/blob/master/lists2/DIY/fixAddLast/SLList.java).

#### Cải tiến #6b: Nút sentinel <a href="#improvement-6b-sentinel-nodes" id="improvement-6b-sentinel-nodes"></a>

Một cách để sửa `addLast` là tạo một nhánh đặc biệt cho danh sách rỗng, như bên dưới:

```java
public void addLast(int x) {
    size += 1;

    if (first == null) {
        first = new IntNode(x, null);
        return;
    }

    IntNode p = first;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

Cách này có tác dụng, nhưng những đoạn mã xử lý nhánh đặc biệt như trên nên được tránh khi có thể. Con người chỉ có một lượng trí nhớ làm việc hữu hạn, nên ta muốn giữ độ phức tạp trong tầm kiểm soát bất cứ khi nào có thể. Với một cấu trúc dữ liệu đơn giản như `SLList`, số lượng nhánh đặc biệt không nhiều. Các cấu trúc dữ liệu phức tạp hơn, như cây, có thể trở nên xấu xí hơn rất nhiều.

Một giải pháp gọn hơn (dù không hiển nhiên bằng) là làm sao cho mọi `SLList` đều "giống nhau", kể cả khi rỗng. Ta làm điều đó bằng cách tạo ra một nút đặc biệt luôn tồn tại, gọi là **nút sentinel**. Nút sentinel sẽ mang một giá trị mà ta không quan tâm.

Ví dụ, một danh sách rỗng tạo bởi `SLList L = new SLList()` sẽ trông như dưới đây:

![empty\_sentinelized\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/empty_sentinelized_SLList.png)

Còn một `SLList` chứa các phần tử 5, 10 và 15 sẽ trông như sau:

![three\_item\_sentenlized\_SLList.png](https://joshhug.gitbooks.io/hug61b/content/chap2/fig22/three_item_sentenlized_SLList.png)

Trong các hình trên, giá trị `??` màu tím nhạt cho biết ta không quan tâm giá trị ở đó là gì. Vì Java không cho phép đặt một số nguyên là dấu hỏi, ta chỉ cần chọn một giá trị tùy ý như `-518273`, `63`, hay bất kỳ gì khác.

Vì một `SLList` có sentinel không còn nhánh đặc biệt nào nữa, ta có thể đơn giản xóa nhánh đặc biệt ra khỏi phương thức `addLast`, thu được:

```java
public void addLast(int x) {
    size += 1;
    IntNode p = sentinel;
    while (p.next != null) {
        p = p.next;
    }

    p.next = new IntNode(x, null);
}
```

Như bạn thấy, đoạn mã này gọn gàng hơn rất nhiều!

#### Bất biến <a href="#invariants" id="invariants"></a>

Bất biến là một mệnh đề về cấu trúc dữ liệu được đảm bảo là đúng (với giả thiết không có lỗi trong mã của bạn).

Một `SLList` có nút sentinel có ít nhất các bất biến sau:

* Tham chiếu `sentinel` luôn trỏ tới một nút sentinel.
* Phần tử đầu tiên (nếu tồn tại) luôn nằm ở `sentinel.next.item`.
* Biến `size` luôn bằng tổng số phần tử đã được thêm vào.

Các bất biến giúp ta suy luận về mã dễ hơn, đồng thời cung cấp những mục tiêu cụ thể mà bạn cần bảo đảm để mã hoạt động đúng.

Để thật sự cảm nhận được sự tiện lợi của sentinel, bạn sẽ phải tự mình cài đặt. Bạn sẽ có nhiều dịp thực hành trong Dự án 1. Tuy nhiên, ta khuyên bạn chờ đến khi đọc xong phần tiếp theo của sách rồi hãy bắt đầu Dự án 1.
