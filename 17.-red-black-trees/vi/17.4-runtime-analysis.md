---
description: Ngắn gọn và súc tích.
---

# 17.4 Phân tích thời gian chạy

Vì cây đỏ-đen nghiêng trái có sự tương ứng 1-1 với cây 2-3 và luôn có chiều cao không vượt quá khoảng gấp đôi chiều cao của cây 2-3 tương ứng, các thao tác sẽ mất $$\log(N)$$ thời gian.

Đây là đoạn mã đã được trừu tượng hóa cho thao tác chèn vào một LLRB:

```
private Node put(Node h, Key key, Value val) {
    if (h == null) { return new Node(key, val, RED); }

    int cmp = key.compareTo(h.key);
    if (cmp < 0)      { h.left  = put(h.left,  key, val); }
    else if (cmp > 0) { h.right = put(h.right, key, val); }
    else              { h.val   = val;                    }

    if (isRed(h.right) && !isRed(h.left))      { h = rotateLeft(h);  }
    if (isRed(h.left)  &&  isRed(h.left.left)) { h = rotateRight(h); }
    if (isRed(h.left)  &&  isRed(h.right))     { flipColors(h);      } 

    return h;
}
```
