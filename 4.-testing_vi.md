# 4. Kiểm thử

### Kiểm thử và Selection Sort <a href="#testing-and-selection-sort" id="testing-and-selection-sort"></a>

Một trong những kỹ năng quan trọng nhất của một lập trình viên từ trình độ trung cấp trở lên là khả năng nhận ra khi nào mã của mình là đúng. Trong chương này, chúng ta sẽ bàn về cách viết kiểm thử để đánh giá tính đúng đắn của mã. Đồng thời, ta cũng sẽ nói về một thuật toán sắp xếp có tên là Selection Sort.

#### Một cách tiếp cận mới <a href="#a-new-way" id="a-new-way"></a>

Khi viết chương trình, bạn có thể mắc lỗi. Trong bối cảnh lớp học, bạn có được sự tự tin về tính đúng đắn của mã thông qua một sự kết hợp nào đó giữa việc tự chạy thử mã, phân tích mã, và kiểm thử bằng autograder; trong nhiều trường hợp, yếu tố cuối cùng này là quan trọng nhất, đặc biệt vì đó là cách bạn kiếm điểm.

Tất nhiên, autograder không phải phép màu. Về bản chất, đó chỉ là mã do giảng viên viết, và nó không khác quá nhiều so với chính đoạn mã bạn đang viết. Trong thế giới thực, các bài kiểm thử này do lập trình viên tự viết, chứ không phải do một bên thứ ba nhân từ kiểu Josh Hug cung cấp.

Trong chương này, chúng ta sẽ khám phá cách tự viết các bài kiểm thử của riêng mình. Mục tiêu của ta là tạo một lớp tên `Sort` cung cấp phương thức `sort(String[] x)` để sắp xếp tại chỗ các chuỗi trong mảng `x`.

Là một cách suy nghĩ hoàn toàn mới, trước tiên chúng ta sẽ bắt đầu bằng cách viết `testSort()` và chỉ sau khi hoàn thành bài kiểm thử, chúng ta mới chuyển sang viết mã sắp xếp thực tế.

#### Kiểm thử ad hoc <a href="#a-d-hoc-testing" id="a-d-hoc-testing"></a>

Viết bài kiểm thử cho `Sort.sort` khá đơn giản, dù hơi tẻ nhạt. Ta chỉ cần tạo dữ liệu đầu vào, gọi `sort`, rồi kiểm tra xem kết quả có đúng không. Nếu kết quả sai, ta in ra vị trí không khớp đầu tiên và dừng kiểm thử. Ví dụ, ta có thể tạo một lớp kiểm thử như sau:

```java
public class TestSort {
    /** Tests the sort method of the Sort class. */
    public static void testSort() {
        String[] input = {"i", "have", "an", "egg"};
        String[] expected = {"an", "egg", "have", "i"};
        Sort.sort(input);
        for (int i = 0; i < input.length; i += 1) {
            if (!input[i].equals(expected[i])) {
                System.out.println("Mismatch in position " + i + ", expected: " + expected + ", but got: " + input[i] + ".");
                break;
            }
        }
    }

    public static void main(String[] args) {
        testSort();
    }
}
```

Ta có thể kiểm tra xem chính bài kiểm thử của mình có hoạt động hay không bằng cách tạo một phương thức `Sort.sort` trống như dưới đây:

```java
public class Sort {
    /** Sorts strings destructively. */
    public static void sort(String[] x) {        
    }
}
```

Nếu chúng ta chạy phương thức `testSort()` với phương thức `Sort.sort` trống này, chúng ta sẽ nhận được:

```sh
Mismatch in position 0, expected: an, but got: i.
```

Việc nhận được thông báo lỗi ở đây là một điều tốt. Nó cho thấy bài kiểm thử đang hoạt động. Điều thú vị là ta đã tự tạo ra cho mình một trò chơi nhỏ: sửa mã trong `Sort.sort` sao cho thông báo lỗi đó biến mất. Đây là một kiểu mẹo tâm lý nho nhỏ, nhưng nhiều lập trình viên thấy việc tự tạo ra những bài toán mini như vậy gần như gây nghiện.

Trên thực tế, điều này khá giống với việc bạn sử dụng autograder trong một môn học và dần bị cuốn vào mục tiêu làm sao để nó “chấm đúng” và công nhận bài làm của mình. Bạn như đang tự tạo ra một “giám khảo” cho chính đoạn code của mình, và cách duy nhất để nhận được sự đánh giá cao từ nó là viết code thật chính xác.

**Lưu ý quan trọng:** Bạn có thể hỏi: "Tại sao phải lặp qua toàn bộ mảng? Tại sao không chỉ xem hai mảng có bằng nhau bằng `==`?". Lý do là khi muốn so sánh hai đối tượng theo *giá trị*, ta không thể chỉ dùng toán tử `==`. Toán tử `==` so sánh trực tiếp các bit trong các hộp bộ nhớ; ví dụ, `input == expected` sẽ cho biết liệu địa chỉ của `input` và `expected` có trùng nhau hay không, chứ không phải nội dung của hai mảng có giống nhau hay không. Vì vậy, ở `testSort` ta dùng một vòng lặp và in ra vị trí không khớp đầu tiên. Bạn cũng có thể dùng phương thức dựng sẵn `java.util.Arrays.equals` thay cho vòng lặp.

Mặc dù bài kiểm thử đơn lẻ ở trên không phải là quá nhiều công việc, nhưng việc viết một bộ các bài kiểm thử _ad hoc_ như vậy sẽ rất tẻ nhạt vì nó sẽ đòi hỏi phải viết một loạt các vòng lặp và câu lệnh in khác nhau. Trong phần tiếp theo, chúng ta sẽ xem thư viện Google Truth giúp chúng ta tiết kiệm rất nhiều công sức như thế nào.

#### Kiểm thử với Truth <a href="#truth-testing" id="truth-testing"></a>

Thư viện Google Truth cung cấp nhiều phương thức và tiện ích hữu ích để đơn giản hóa việc viết kiểm thử. Ví dụ, ta có thể thay bài kiểm thử _ad hoc_ đơn giản ở trên bằng:

```java
import static com.google.common.truth.Truth.assertThat;
public class TestSort {
   /** Tests the sort method of the Sort class. */
   public static void testSort() {
       String[] input = {"cows", "dwell", "above", "clouds"};
       String[] expected = {"above", "clouds", "cows", "dwell"};
       Sort.sort(input);

       assertThat(input).isEqualTo(expected);
   }

   public static void main(String[] args) {
       testSort();
   }
}

```

**Đoạn mã này đơn giản hơn nhiều**, nhưng về cơ bản làm cùng một việc: nếu hai mảng không bằng nhau, nó sẽ chỉ ra vị trí không khớp đầu tiên. Ví dụ, nếu ta chạy `testSort()` trên một phương thức `Sort.sort` không làm gì cả, ta sẽ nhận được:

```sh
Exception in thread "main" arrays first differed at element [0]; expected:<[an]> but was:<[i]>
    at org.junit.internal.ComparisonCriteria.arrayEquals(ComparisonCriteria.java:55)
    at org.junit.Assert.internalArrayEquals(Assert.java:532)
    ...
```

Mặc dù đầu ra này hơi xấu hơn so với bài kiểm thử _ad hoc_, ta sẽ thấy ở cuối chương cách làm cho nó đẹp hơn.

#### Selection Sort <a href="#selection-sort" id="selection-sort"></a>

Trước khi có thể viết phương thức `Sort.sort`, chúng ta cần một số thuật toán để sắp xếp. Có lẽ thuật toán sắp xếp đơn giản nhất là "sắp xếp lựa chọn". Sắp xếp lựa chọn bao gồm ba bước:

* Tìm phần tử nhỏ nhất.
* Đưa nó ra đầu.
* Thực hiện selection sort trên `N - 1` phần tử còn lại, không động vào phần tử đầu.

Ví dụ, giả sử ta có mảng `{6, 3, 7, 2, 8, 1}`. Phần tử nhỏ nhất trong mảng này là `1`, nên ta sẽ đưa `1` ra đầu. Có hai cách tự nhiên để làm điều này: một là đặt `1` vào đầu rồi dồn tất cả các số còn lại sang phải, tức là `{1, 6, 3, 7, 2, 8}`. Tuy nhiên, cách hiệu quả hơn nhiều là chỉ cần hoán đổi `1` với phần tử đang đứng đầu cũ, ở đây là `6`, để thu được `{1, 3, 7, 2, 8, 6}`.

Ta chỉ cần lặp lại quy trình tương tự cho các chữ số còn lại, tức là phần tử nhỏ nhất trong `... 3, 7, 2, 8, 6}` là `2`. Hoán đổi nó ra đầu, ta được `{1, 2, 7, 3, 8, 6}`. Lặp tiếp cho đến khi mảng được sắp xếp hoàn toàn, ta lần lượt có `{1, 2, 3, 7, 8, 6}`, rồi `{1, 2, 3, 6, 8, 7}`, và cuối cùng là `{1, 2, 3, 6, 7, 8}`.

Về mặt toán học, chúng ta có thể chứng minh tính đúng đắn của thuật toán sắp xếp này trên bất kỳ mảng nào bằng cách sử dụng khái niệm bất biến đã được giới thiệu lần đầu trong chương 2.4, mặc dù chúng ta sẽ không làm như vậy trong sách giáo khoa này. Trước khi tiếp tục, hãy thử viết ra dãy số ngắn của riêng bạn và thực hiện sắp xếp lựa chọn trên đó để bạn có thể chắc chắn rằng mình hiểu được ý tưởng.

Bây giờ chúng ta đã biết cách sắp xếp lựa chọn hoạt động như thế nào, chúng ta có thể viết một vài nhận xét ngắn bằng phương thức `Sort.sort` trống để hướng dẫn suy nghĩ của chúng ta:

```java
public class Sort {
    /** Sorts strings destructively. */
    public static void sort(String[] x) { 
           // find the smallest item
           // move it to the front
           // selection sort the rest (using recursion?)
    }
}
```

Trong các phần sau, tôi sẽ thử hoàn thành phần cài đặt của selection sort theo cách mà một sinh viên có thể tiếp cận bài toán này. Vì thế, **tôi sẽ cố ý mắc một vài lỗi trên đường đi**. Những lỗi có chủ đích này là điều tốt, vì chúng giúp minh họa tính hữu ích của kiểm thử. Nếu bạn phát hiện ra lỗi nào khi đọc, đừng lo; rồi ta sẽ quay lại và sửa chúng.

#### findSmallest <a href="#findsmallest" id="findsmallest"></a>

Cách tự nhiên nhất để bắt đầu là viết một phương thức tìm phần tử nhỏ nhất trong danh sách. Cũng như với `Sort.sort`, ta sẽ viết bài kiểm thử trước khi hoàn tất phương thức. Đầu tiên, ta tạo một phiên bản giả của `findSmallest`, đơn giản chỉ trả về một giá trị tùy ý:

```java
public class Sort {
    /** Sorts strings destructively. */
    public static void sort(String[] x) { 
           // find the smallest item
           // move it to the front
           // selection sort the rest (using recursion?)
    }

    /** Returns the smallest string in x. */
    public static String findSmallest(String[] x) {
        return x[2];
    }
}
```

Rõ ràng đây không phải là một cách cài đặt đúng, nhưng ta cố tình hoãn việc suy nghĩ về cách `findSmallest` nên hoạt động cho đến sau khi viết xong bài kiểm thử. Dùng thư viện `org.junit`, việc thêm một bài kiểm thử như vậy vào lớp `TestSort` khá dễ, như minh họa dưới đây:

```java
public class TestSort {
   @Test
   public void testFindSmallest() {
       String[] input = {"rawr", "a", "zaza", "newway"};
       String expected = "zaza";
       String actual = Sort.findSmallest(input);
       assertThat(actual).isEqualTo(expected);
   }
}
```

Giống như với `TestSort.testSort`, ta chạy `TestSort.testFindSmallest` để chắc rằng nó thất bại. Nhưng khi chạy bài kiểm thử này, ta sẽ thấy nó lại vượt qua, tức là chẳng có thông báo nào hiện ra. Lý do là ta đã vô tình hard-code đúng giá trị trả về là `x[2]`. Vậy hãy sửa `findSmallest` để nó trả về một kết quả chắc chắn sai:

```java
/** Returns the smallest string in x. */
public static String findSmallest(String[] x) {
    return x[3];
}
```

Sau khi thay đổi như vậy, khi chạy `TestSort.testFindSmallest`, ta sẽ nhận được lỗi, và đó là một điều tốt:

```sh
Exception in thread "main" java.lang.AssertionError: expected:<[an]> but was:<[null]>
    at org.junit.Assert.failNotEquals(Assert.java:834)
    at TestSort.testFindSmallest(TestSort.java:9)
    at TestSort.main(TestSort.java:24)
```

Như trước đây, chúng ta đã thiết lập cho mình một trò chơi nhỏ để chơi, mục tiêu của chúng ta bây giờ là sửa đổi mã cho `Sort.findSmallest` để lỗi này không còn xuất hiện nữa. Đây là một mục tiêu nhỏ hơn so với việc làm cho `Sort.sort` hoạt động, mục tiêu này thậm chí có thể gây nghiện hơn.

Lưu ý bên lề: Có vẻ khá giả tạo khi tôi tình cờ trả về đúng giá trị `x[2]`. Tuy nhiên, khi tôi quay video bài giảng này, tôi thực sự đã mắc phải lỗi này mà không hề có ý định làm như vậy!

Tiếp theo chúng ta chuyển sang viết `findSmallest`. Điều này có vẻ như nó tương đối đơn giản. Nếu bạn là người mới sử dụng Java, bạn có thể sẽ viết mã trông giống như thế này:

```java
/**  Returns the smallest string in x. */
public static String findSmallest(String[] x) {
    String smallest = x[0];
    for (int i = 0; i < x.length; i += 1) {
        if (x[i] < smallest) {
            smallest = x[i];
        }
    }
    return smallest;
}
```

Tuy nhiên, điều này sẽ gây ra lỗi biên dịch "< không thể áp dụng cho 'java.lang.String'". Vấn đề là kiểm tra kiểu tĩnh của Java không cho phép so sánh các `String` bằng toán tử `<`.

Khi lập trình mà mắc kẹt ở một vấn đề có thể mô tả ngắn gọn như thế này, lựa chọn tốt nhất thường là dùng công cụ tìm kiếm. Ví dụ, ta có thể tìm "less than strings Java" trên Google. Một truy vấn như vậy có thể dẫn đến một bài Stack Overflow như [bài này](https://stackoverflow.com/questions/5153496/how-can-i-compare-two-strings-in-java-and-define-which-of-them-is-smaller-than-t).

Một trong những câu trả lời phổ biến cho bài đăng này giải thích rằng phương thức `str1.compareTo(str2)` sẽ trả về số âm nếu `str1 < str2`, 0 nếu chúng bằng nhau và số dương nếu `str1 > str2`.

Áp dụng điều đó vào mã của mình, ta có thể thu được:

```java
/** Returns the smallest string in x. 
  * @source Got help with string compares from https://goo.gl/a7yBU5. */
public static String findSmallest(String[] x) {
    String smallest = x[0];
    for (int i = 0; i < x.length; i += 1) {
        int cmp = x[i].compareTo(smallest);
        if (cmp < 0) {
            smallest = x[i];
        }
    }
    return smallest;
}
```

Lưu ý rằng ở đây ta dùng thẻ `@source` để ghi tham chiếu nguồn. Tôi đưa ví dụ này chủ yếu cho những bạn đang học 61B như một môn chính thức. Đây không phải là thực hành điển hình trong thế giới thực.

Vì ta đang dùng các tính năng cú pháp hoàn toàn mới nên có thể thiếu tin tưởng vào tính đúng đắn của phương thức `findSmallest`. May mắn thay, chúng ta vừa mới viết bài kiểm thử đó cách đây ít lâu. Nếu thử chạy nó, chúng ta sẽ thấy không có gì được in, điều đó có nghĩa là mã của chúng ta có thể đúng.

Chúng ta có thể củng cố bộ kiểm thử để tăng độ tin cậy bằng cách thêm nhiều trường hợp kiểm thử hơn. Ví dụ: ta có thể sửa `testFindSmallest` để nó trông như đoạn hiển thị bên dưới:

```java
public static void testFindSmallest() {
    String[] input = {"i", "have", "an", "egg"};
    String expected = "an";

    String actual = Sort.findSmallest(input);
    assertThat(actual).isEqualTo(expected);     

    String[] input2 = {"there", "are", "many", "pigs"};
    String expected2 = "are";

    String actual2 = Sort.findSmallest(input2);
    assertThat(actual2).isEqualTo(expected2);  
}
```

Chạy lại bài kiểm thử, chúng ta thấy rằng nó vẫn đạt. Chúng ta không hoàn toàn chắc rằng `findSmallest` đúng trong mọi tình huống, nhưng chắc chắn hơn nhiều so với khi không có kiểm thử bổ sung.

#### Hoán đổi <a href="#swap" id="swap"></a>

Nhìn vào phương thức `sort` của ta bên dưới, phương thức trợ giúp tiếp theo cần viết là một phương thức để đưa một phần tử lên phía trước, mà ta sẽ gọi là `swap`.

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
       // find the smallest item
       // move it to the front
       // selection sort the rest (using recursion?)
}
```

Viết một phương thức `swap` rất đơn giản và có thể bạn đã từng làm như vậy trước đây. Việc triển khai đúng có thể trông giống như:

```java
public static void swap(String[] x, int a, int b) {
    String temp = x[a];
    x[a] = x[b];
    x[b] = temp;
}
```

Tuy nhiên, ở thời điểm này, hãy cố ý đưa vào một lỗi để ta có thể minh họa tính hữu ích của kiểm thử. Một lập trình viên thiếu kinh nghiệm hơn có thể viết như sau:

```java
public static void swap(String[] x, int a, int b) {    
    x[a] = x[b];
    x[b] = x[a];
}
```

Viết bài kiểm thử cho phương thức này khá dễ dàng với sự trợ giúp của JUnit. Một bài kiểm thử mẫu được hiển thị dưới đây. Lưu ý rằng chúng ta cũng đã chỉnh sửa phương thức `main` để nó gọi `testSwap` thay vì `testFindSmallest` hoặc `testSort`.

```java
public class TestSort {
    ...    

    /** Test the Sort.swap method. */
    public static void testSwap() {
        String[] input = {"i", "have", "an", "egg"};
        int a = 0;
        int b = 2;
        String[] expected = {"an", "have", "i", "egg"};

        Sort.swap(input, a, b);
        assertThat(expected).isEqualTo(input);
    }

    public static void main(String[] args) {
        testSwap();
    }
}
```

Việc chạy bài kiểm thử này trên lỗi `swap` của ta sẽ gây ra lỗi như ta mong đợi.

```sh
Exception in thread "main" arrays first differed in element [2]; expected:<[i]> but was:<[an]>
    at TestSort.testSwap(TestSort.java:36)
```

Cần lưu ý ngắn gọn rằng điều quan trọng là chúng ta chỉ gọi `testSwap` chứ không phải `testSort`. Ví dụ: nếu phương thức `main` của ta như dưới đây, thì toàn bộ phương thức `main` sẽ chấm dứt thực thi ngay khi `testSort` không thành công và `testSwap` sẽ không bao giờ chạy:

```java
public static void main(String[] args) {
    testSort();
    testFindSmallest();
    testSwap();
}
```

Chúng ta sẽ tìm hiểu một cách tinh tế hơn để xử lý nhiều bài kiểm thử ở cuối chương này, tránh phải chỉ định thủ công bài nào sẽ chạy.

Bây giờ chúng ta có một bài kiểm thử thất bại, chúng ta có thể sử dụng nó để giúp chúng ta gỡ lỗi. Một cách để thực hiện việc này là đặt điểm dừng bên trong phương thức `swap` và sử dụng tính năng gỡ lỗi trực quan trong IntelliJ. Nếu bạn muốn biết thêm thông tin và thực hành gỡ lỗi, hãy xem [Phòng thí nghiệm 3](https://sp19.datastructur.es/materials/lab/lab3/lab3). Việc xem qua từng dòng mã sẽ giúp bạn thấy ngay điều gì sai (xem video hoặc tự mình thử), rồi ta có thể sửa bằng cách cập nhật mã để dùng biến tạm như ở đầu mục này:

```java
public static void swap(String[] x, int a, int b) {
    String temp = x[a];
    x[a] = x[b];
    x[b] = temp;
}
```

Chạy lại bài kiểm thử, chúng ta thấy rằng nó hiện đã vượt qua.

#### Đang sửa đổi findSmallest <a href="#revising-findsmallest" id="revising-findsmallest"></a>

Bây giờ chúng ta đã hoàn thành nhiều phần của phương thức, chúng ta có thể bắt đầu thử kết nối chúng lại với nhau để tạo ra phương thức `Sort.sort`.

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
       // find the smallest item
       // move it to the front
       // selection sort the rest (using recursion?)
}
```

Rõ ràng về cách sử dụng các phương thức `findSmallest` và `swap`, nhưng khi làm như vậy, ta ngay lập tức nhận ra có một chút không khớp: `findSmallest` trả về một `String` và `swap` mong đợi hai chỉ số.

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
       // find the smallest item
       String smallest = findSmallest(x);

       // move it to the front
       swap(x, 0, smallest);

       // selection sort the rest (using recursion?)
}
```

Nói cách khác, thứ mà `findSmallest` lẽ ra phải trả về là chỉ mục của Chuỗi nhỏ nhất, chứ không phải chính Chuỗi đó. Việc mắc phải những lỗi ngớ ngẩn như thế này là điều bình thường và thực sự dễ mắc phải, vì vậy đừng lo lắng nếu bạn thấy mình cũng làm điều gì đó tương tự. Lặp lại trên một thiết kế là một phần của quá trình viết mã.

May mắn thay, thiết kế mới này có thể dễ dàng thay đổi. Chúng ta chỉ cần điều chỉnh `findSmallest` để trả về `int`, như hiển thị bên dưới:

```java
public static int findSmallest(String[] x) {
    int smallestIndex = 0;
    for (int i = 0; i < x.length; i += 1) {
        int cmp = x[i].compareTo(x[smallestIndex]);
        if (cmp < 0) {
            smallestIndex = i;
        }
    }
    return smallestIndex;
}
```

Vì đây là một thay đổi không hề nhỏ nên ta cũng nên cập nhật `testFindSmallest` và đảm bảo rằng `findSmallest` vẫn hoạt động.

```java
public static void testFindSmallest() {
    String[] input = {"i", "have", "an", "egg"};
    int expected = 2;

    int actual = Sort.findSmallest(input);
    assertThat(actual).isEqualTo(expected); 

    String[] input2 = {"there", "are", "many", "pigs"};
    int expected2 = 1;

    int actual2 = Sort.findSmallest(input);
    assertThat(actual2).isEqualTo(expected2); 
}
```

Sau khi sửa đổi `TestSort` để chạy bài kiểm thử này và chạy `TestSort.main`, chúng ta thấy rằng mã đã vượt qua các bài kiểm thử. Bây giờ, khi sửa lại `sort`, chúng ta có thể điền vào hai bước đầu tiên của thuật toán selection sort.

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
   // find the smallest item
   // move it to the front
   // selection sort the rest (using recursion?)
   int smallestIndex = findSmallest(x);
   swap(x, 0, smallestIndex);
}
```

Tất cả những gì còn lại là bằng cách nào đó lựa chọn sắp xếp các mục còn lại, có thể sử dụng đệ quy. Chúng ta sẽ giải quyết vấn đề này trong phần tiếp theo.

Suy ngẫm về những gì chúng ta đã đạt được, cần lưu ý cách ta viết kiểm thử trước và dùng chúng để xây dựng niềm tin rằng các phương thức thực thể hoạt động đúng trước khi ghép chúng vào mục đích lớn hơn. Đây là một ý tưởng cực kỳ quan trọng và sẽ có ích cho bạn nếu bạn quyết định áp dụng nó.

#### Các phương thức trợ giúp đệ quy <a href="#recursive-helper-methods" id="recursive-helper-methods"></a>

Để bắt đầu phần này, hãy nghĩ xem bạn có thể thực hiện lời gọi đệ quy cần thiết để hoàn thành `sort` như thế nào:

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
   int smallestIndex = findSmallest(x);
   swap(x, 0, smallestIndex);
   // recursive call??
}
```

Đối với những người đã quen với ngôn ngữ như Python, có thể bạn sẽ muốn thử và sử dụng thứ gì đó như ký hiệu lát cắt, ví dụ:

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
   int smallestIndex = findSmallest(x);
   swap(x, 0, smallestIndex);
   sort(x[1:])
}
```

Tuy nhiên, trong Java không có thứ nào tham chiếu đến mảng con, tức là chúng ta không thể chuyển địa chỉ của mục tiếp theo trong mảng.

Vấn đề chỉ cần xem xét một tập hợp con của một mảng lớn hơn là rất phổ biến. Một giải pháp điển hình là tạo một phương thức trợ giúp riêng có một tham số (hoặc các tham số) bổ sung để mô tả phần nào của mảng cần xem xét. Ví dụ: chúng ta có thể viết một phương thức trợ giúp riêng còn được gọi là `sort` chỉ xem xét các mục bắt đầu bằng mục `start`.

```java
/** Sorts strings destructively starting from item start. */
private static void sort(String[] x, int start) { 
    // TODO
}
```

Không giống như phương thức `sort` công khai của ta, việc dùng đệ quy giờ tương đối đơn giản khi ta có tham số bổ sung `start`, như hiển thị bên dưới. Ta sẽ kiểm thử phương thức này trong phần tiếp theo.

```java
/** Sorts strings destructively starting from item start. */
private static void sort(String[] x, int start) { 
   int smallestIndex = findSmallest(x);
   swap(x, start, smallestIndex);
   sort(x, start + 1);
}
```

Bây giờ chúng ta đã có một phương thức trợ giúp, chúng ta cần thiết lập lệnh gọi ban đầu chính xác. Nếu chúng ta đặt phần đầu thành 0, chúng ta sẽ sắp xếp toàn bộ mảng một cách hiệu quả.

```java
/** Sorts strings destructively. */
public static void sort(String[] x) { 
   sort(x, 0);
}
```

Cách tiếp cận này khá phổ biến khi cố gắng sử dụng đệ quy trên cấu trúc dữ liệu vốn không phải là đệ quy, ví dụ: mảng.

#### Gỡ lỗi và hoàn thành sắp xếp <a href="#debugging-and-completing-sort" id="debugging-and-completing-sort"></a>

Khi chạy phương thức `testSort`, ta ngay lập tức gặp phải một vấn đề:

```sh
Exception in thread "main" java.lang.ArrayIndexOutOfBoundsException: 4
    at Sort.swap(Sort.java:16)
```

Khi sử dụng trình gỡ lỗi Java, chúng ta thấy rằng vấn đề là bằng cách nào đó `start` đang đạt đến giá trị 4. Cẩn thận xem qua mã (xem video ở trên), ta thấy rằng vấn đề là ta đã quên đưa trường hợp cơ sở vào phương thức `sort` đệ quy. Việc khắc phục điều này rất đơn giản:

```java
/** Sorts strings destructively starting from item start. */
private static void sort(String[] x, int start) { 
   if (start == x.length) {
       return;
   }
   int smallestIndex = findSmallest(x);
   swap(x, start, smallestIndex);
   sort(x, start + 1);
}
```

Chạy lại bài kiểm thử này một lần nữa, ta gặp một lỗi khác:

```sh
Exception in thread "main" arrays first differed at element [0]; 
   expected<[an]> but was:<[have]>
```

Một lần nữa, với việc sử dụng hợp lý trình gỡ lỗi IntelliJ (xem video), chúng ta có thể xác định một dòng mã có kết quả không phù hợp với mong đợi của chúng ta. Điều đáng chú ý là thực tế là tôi đã gỡ lỗi mã ở mức độ trừu tượng cao hơn mức bạn có thể có, điều này tôi đạt được bằng cách sử dụng `Step Over` nhiều hơn `Step Into`. Như đã thảo luận trong phòng thí nghiệm 3, việc gỡ lỗi ở mức độ trừu tượng cao hơn giúp bạn tiết kiệm rất nhiều thời gian và năng lượng bằng cách cho phép bạn so sánh kết quả của toàn bộ lệnh gọi hàm với mong đợi của bạn.

Cụ thể, chúng ta nhận thấy rằng khi sắp xếp 3 (trong số 4) mục cuối cùng, phương thức `findSmallest` sẽ đưa ra mục thứ 0 (`"an"`) thay vì mục thứ 3 (`"egg"`) khi được gọi trên đầu vào `{"an", "have", "i", "egg"}`. Xem xét kỹ định nghĩa của `findSmallest`, hành vi này không có gì đáng ngạc nhiên, vì `findSmallest` xem xét toàn bộ mảng chứ không chỉ các mục bắt đầu từ chỉ số `start`. Loại lỗi thiết kế này rất phổ biến và viết bài kiểm thử cũng như dùng trình gỡ lỗi là một cách tuyệt vời để khắc phục.

Để sửa mã, ta sửa lại `findSmallest` để nó nhận thêm tham số `start`, tức là `findSmallest(String[] x, int start)`. Như vậy ta chỉ tìm phần tử nhỏ nhất trong phần đuôi chưa sắp xếp (bất kể còn bao nhiêu phần tử). Bản sửa đổi như dưới đây:

```java
public static int findSmallest(String[] x, int start) {
    int smallestIndex = start;
    for (int i = start; i < x.length; i += 1) {
        int cmp = x[i].compareTo(x[smallestIndex]);
        if (cmp < 0) {
            smallestIndex = i;
        }
    }
    return smallestIndex;
}
```

Vì ta đã thay đổi đáng kể một trong các khối xây dựng, tức là `findSmallest`, ta nên đảm bảo rằng các sửa đổi là chính xác.

Trước tiên, ta sửa `testFindSmallest` để dùng tham số mới, như hiển thị bên dưới:

```java
public static void testFindSmallest() {
    String[] input = {"i", "have", "an", "egg"};
    int expected = 2;

    int actual = Sort.findSmallest(input, 0);
    assertThat(actual).isEqualTo(expected);        

    String[] input2 = {"there", "are", "many", "pigs"};
    int expected2 = 2;

    int actual2 = Sort.findSmallest(input2, 2);
    assertThat(actual2).isEqualTo(expected2); 
}
```

Sau đó, chúng ta sửa đổi `TestSort.main` để nó chạy `testFindSmallest`. Bài kiểm thử này đã vượt qua, cho thấy rõ ràng rằng các sửa đổi của ta đối với `findSmallest` là chính xác.

Tiếp theo chúng ta sửa đổi `Sort.sort` để nó sử dụng tham số `start` mới trong `findSmallest`:

```java
/** Sorts strings destructively starting from item start. */
private static void sort(String[] x, int start) { 
   if (start == x.length) {
       return;
   }
   int smallestIndex = findSmallest(x, start);
   swap(x, start, smallestIndex);
   sort(x, start + 1);
}
```

Sau đó, ta sửa `TestSort` để nó chạy `TestSort.sort` và thế là phương thức này hoạt động. Chúng ta đã xong! Bây giờ bạn đã thấy “cách mới” ngay từ đầu bài giảng này, điều mà chúng ta sẽ suy ngẫm trong phần còn lại của chương này.

#### Những suy nghĩ về quá trình phát triển <a href="#reflections-on-the-development-process" id="reflections-on-the-development-process"></a>

Khi bạn viết và gỡ lỗi một chương trình, bạn sẽ thường thấy mình chuyển đổi giữa các ngữ cảnh khác nhau. Cố gắng giữ quá nhiều thứ trong đầu cùng một lúc là công thức dẫn đến thảm họa tồi tệ nhất và tốt nhất là làm chậm tiến độ.

Việc có một bộ kiểm thử tự động giúp giảm tải nhận thức này. Ví dụ, khi đang viết `sort`, ta phát hiện có lỗi trong `findSmallest`. Ta có thể chuyển ngữ cảnh sang xem xét `findSmallest`, xác nhận nó đúng bằng phương thức `testFindSmallest`, rồi quay lại `sort`. Điều này hoàn toàn trái ngược với một cách tiếp cận thiếu chặt chẽ hơn, nơi bạn chỉ liên tục gọi `sort` và cố đoán xem hành vi của toàn bộ thuật toán có cho thấy `findSmallest` là đúng hay không.

Tương tự, bạn có thể kiểm tra xem dây dù của dù có hoạt động hay không bằng cách lên máy bay, cất cánh, nhảy ra ngoài và kéo dây dù và xem liệu chiếc dù có bung ra hay không. Tuy nhiên, bạn cũng có thể kéo nó xuống đất và xem điều gì sẽ xảy ra. Vì vậy, có cần thiết phải sử dụng `sort` để thử `findSmallest` hay không.

Như đã đề cập trước đó trong chương này, các bài kiểm thử cũng cho phép bạn có được sự tự tin về các phần cơ bản của chương trình, để nếu có sự cố xảy ra, bạn sẽ có ý tưởng tốt hơn về việc bắt đầu tìm kiếm từ đâu.

Cuối cùng, các bài kiểm thử giúp việc cấu trúc lại mã của bạn dễ dàng hơn. Giả sử bạn quyết định viết lại `findSmallest` để nó nhanh hơn hoặc dễ đọc hơn. Chúng ta có thể làm điều đó một cách an toàn bằng cách thực hiện những thay đổi mong muốn và xem liệu bộ kiểm thử có còn vượt qua hay không.

#### Thêm tính năng kiểm thử (JUnit)

Nếu chúng ta thêm `@Test` trước một phương thức và đặt phương thức đó thành không `static` (phương thức thực thể), các mũi tên màu xanh lục sẽ xuất hiện.

* Mũi tên màu xanh lục duy nhất của testSort có nghĩa là “chạy chức năng này”.
* Mũi tên kép màu xanh lục có nghĩa là chạy tất cả các bài kiểm thử trong lớp này.

![](https://lh7-us.googleusercontent.com/AxG53TwAIPggHLPD_XgOBQkOXuaAfUs5lyTY7_WBhpeS8EmNiNFppveTHIXU0HUgji_NxgpI6v6wAfGJfQuMDoRkSFm78_fL5PT_1QkjfiEyrTv7GSIPhWOSX9P_RhKgw6XKc_-M0K-992_ktzGMtEokSQ=s2048)

Lý do tại sao phương thức phải không `static` vẫn chưa rõ ràng, mặc dù điều này có thể liên quan đến những gì IntelliJ làm ở hậu trường.

Một lợi ích bổ sung của việc này là giờ đây IntelliJ sẽ ứng dụng việc sửa lỗi và thiết kế. Bạn có những mục tiêu nhỏ cụ thể và tiến trình của bạn được tóm tắt ở phía dưới bên trái. Bạn giành chiến thắng khi nhận được séc xanh cho mỗi bài kiểm thử.

![](https://lh7-us.googleusercontent.com/BRu_eKYg7yRI7jAqgGXnHzN4CvNiKWtVMq-SbDC9vzjS2Vd8rNbGB-GBUWcGxyinejZWzO-dmWbJ2WGa3jRDpJ56yPXHExjkUJYfCL46V7stzGSFT2dOO0FsrIQqgOLdE6iFfrVvCfr0n6IN1EjQcRXYnw=s2048)

#### Triết lý kiểm thử <a href="#testing-philosophy" id="testing-philosophy"></a>

**Công cụ đánh giá độ đúng số 1: Autograder**

Hãy quay trở lại mặt đất số 0. Autograder có thể là công cụ đầu tiên giúp bạn xác minh mã đúng. Trên thực tế, autograder của khóa học dựa trên JUnit cùng với một số thư viện tùy chỉnh bổ sung.

Có một số lợi ích tuyệt vời khi có autograder. Có lẽ quan trọng nhất là nó xác minh tính chính xác cho bạn, giúp bạn thoát khỏi công việc tẻ nhạt và không mang tính hướng dẫn khi viết tất cả các bài kiểm thử của riêng bạn. Nó cũng ứng dụng quá trình đánh giá bằng cách đưa ra những điểm hấp dẫn như một động lực để đạt được sự đúng đắn. Điều này cũng có thể phản tác dụng nếu học sinh dành quá nhiều thời gian để theo đuổi điểm cuối kỳ mà không thực sự ảnh hưởng đến điểm số hoặc việc học của các em.

Tuy nhiên, autograder không tồn tại trong thế giới thực và phụ thuộc autograder có thể tạo thói quen xấu. Quy trình làm việc bị kéo chậm vì phải thỉnh thoảng tải mã lên và chờ autograder chạy. _Phát triển hướng autograder_ là biến thể cực đoan: sinh viên viết hết mã, sửa lỗi biên dịch, rồi nộp cho autograder; nhận lỗi về thì sửa vài chỗ, thêm `print`, nộp lại—lặp đi lặp lại. Cuối cùng, bạn không làm chủ được quy trình hay mã nếu cứ dựa vào autograder.

**Công cụ đánh giá độ đúng số 2: Kiểm thử JUnit**

Kiểm thử với JUnit, như ta đã thấy, mở ra một thế giới mới cho bạn. Thay vì dựa vào autograder do người khác viết, bạn tự viết bài kiểm thử cho từng phần của chương trình. Ta coi mỗi phần đó là một đơn vị. Điều này cho phép bạn tin tưởng từng đơn vị mã—bạn có thể tin cậy chúng. Điều này cũng giúp giảm thời gian gỡ lỗi vì bạn có thể tập trung vào một đơn vị mã tại một thời điểm (thường là một phương thức duy nhất). Kiểm thử đơn vị cũng buộc bạn phải làm rõ mỗi đơn vị mã cần hoàn thành nhiệm vụ gì.

Tuy nhiên, có một số nhược điểm đối với bài kiểm thử đơn vị. Đầu tiên, viết bài kiểm thử kỹ lưỡng cần có thời gian. Thật dễ dàng để viết các bài kiểm thử đơn vị không đầy đủ, khiến mã của bạn bị sai lệch. Cũng khó viết bài kiểm thử cho các đơn vị phụ thuộc vào các đơn vị khác (hãy xem xét phương thức `addFirst` trong `LinkedListDeque` của bạn).

_**Phát triển hướng kiểm thử (TDD)**_

TDD là một quy trình trong đó ta viết mã kiểm thử trước khi viết mã được kiểm thử. Các bước thực hiện như sau:

1. Xác định một tính năng mới.
2. Viết một bài kiểm thử đơn vị cho tính năng đó.
3. Chạy bài kiểm thử. Nó sẽ thất bại.
4. Viết mã để bài kiểm thử vượt qua. Đúng!
5. Tùy chọn: tái cấu trúc mã cho nhanh hơn, sạch hơn, v.v.—kèm theo đó là các bài kiểm thử vẫn vượt qua.

TDD không bắt buộc trong lớp này và có thể không phải phong cách của bạn, nhưng kiểm thử đơn vị nói chung chắc chắn là một ý tưởng hay.

**Công cụ đánh giá độ đúng số 3: Kiểm thử tích hợp**

Kiểm thử đơn vị là rất tốt nhưng chúng ta cũng nên đảm bảo rằng các đơn vị này hoạt động bình thường cùng nhau ([không giống meme này](https://media.giphy.com/media/3o7rbPDRHIHwbmcOBy/giphy.gif)). Kiểm thử tích hợp xác minh rằng các thành phần tương tác đúng cách với nhau. JUnit trên thực tế có thể được sử dụng cho việc này. Bạn có thể tưởng tượng kiểm thử đơn vị là lớp cơ bản nhất, còn kiểm thử tích hợp ở mức trừu tượng cao hơn.

Thách thức với kiểm thử tích hợp là việc thực hiện thủ công rất tẻ nhạt nhưng lại khó tự động hóa. Và ở mức độ trừu tượng cao, bạn rất dễ bỏ sót những lỗi nhỏ hoặc hiếm gặp.

Tóm lại, bạn **chắc chắn nên viết bài kiểm thử nhưng chỉ khi chúng có thể hữu ích!** Lấy cảm hứng từ TDD, việc viết bài kiểm thử trước khi viết mã cũng có thể rất hữu ích trong một số trường hợp.
